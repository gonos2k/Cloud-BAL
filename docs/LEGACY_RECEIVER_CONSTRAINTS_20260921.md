# Legacy receiver constraints — 2026-09-21

## Scope and frozen inputs

PR #29 (`3a48720e03792e53af0de4230373461d40171b66`) keeps its scoped closures:
NaN influence rejection, shared `A=DG`, face extrema and reversible nullspace
checks. This follow-up changes diagnostic code and plans only. Production Fortran,
RHS, face mobility, boundary authority, tolerances and 10 m/s / 5 Pa/s gates are
unchanged. No full BALCON, balance publication, ON, native startup or forecast is
performed. The historical bounded candidate is replayed, not regenerated by a new
optimizer.

| Input | SHA256 |
|---|---|
| Production `src/balance/qbalpe.f` | `63a8fd2e1f7a0025e02efa91d77bfe576452c83810f7ba676ee936dc97b60e17` |
| Actual snapshot | `96932e53e5f30bf3e9c307617765aaa7200750ec01d4ead6b45842faf97fa661` |
| Actual 723,638-row export | `dceb0827dc26bd09510568a9ae1bfe6889c1d09a9a809d738559ac37a278b335` |

The diagnostic uses the actual legacy metric `dx=dy=h`, `z=h*dp` and valid
six-donor pressure rows. These are discrete cancellation weights, not native dry
mass or cell volumes. Weighted sums below are not kg/s.

## Fixed receiver rows and stored-state replay

`P` is the original 723,638 active rows. `R` is the union of all valid inactive
rows incident to the 80,517 hypothetical pure-support omega controls, including
controls whose candidate change is zero. `R` contains **79,316 unique rows**.
It is not selected from whichever rows improve, nor from only changed faces.
Every receiver has beta zero, lies above its pressure surface and has all six
valid donors. The hypothetical face inventory does not grant production authority.

Compute on the same rows and metric:

```text
r_before = D_R(y_proposed)
r_after  = D_R(y_stored_candidate)
delta_r  = r_after - r_before
```

The stored candidate was created in PR #29. Its original source/hash receipts are
preserved separately from this new replay. All values here are recalculated from
the actual float32 state converted to float64 before subtraction and divergence.

| Receiver quantity | Maximum absolute value (s^-1) | RMS (s^-1) |
|---|---:|---:|
| Before | 6.835067546e-4 | 4.769297531e-5 |
| Change | 1.996000078e-3 | 2.603053961e-4 |
| After | 2.136711690e-3 | 2.662726564e-4 |

Absolute residual improves in **15,028** rows, worsens in **54,200**, and is
unchanged in **10,088**. Thus 69,228 rows change. The candidate does not worsen
every receiver, but this fixed set's RMS grows. The earlier `1.995999999923e-3`
value described the precast change; `1.996000078e-3` here describes the actual
stored-state difference. Neither alone is the final receiver residual.

A separate fresh-scratch pinned Intel O0/O2 replay uses the unchanged production
`continuity_point` and `qbal_divergence_value` procedure bodies. On every receiver,
float64 before/after results agree exactly with the Python calculation; maximum
difference is 0 at both optimization levels. The valid-donor and inactive-row
checks pass. This is a source-matched procedure replay, not a full executable run.

## Necessary conditions before optimization

For the fixed hypothetical face set, every changed face is either internal to P
or shared between P and R. Its signed weighted contributions cancel:

```text
z_P^T D_P q + z_R^T D_R q = 0.
```

For exact active target `D_P q=b_P`, set `d=z_P^T b_P`,
`B=z_R^T r_before` and `W=sum(z_R)`. Then

```text
z_R^T delta_r = -d
z_R^T r_after = B-d
max(abs(delta_r)) >= abs(d)/W
max(abs(r_after)) >= abs(B-d)/W.
```

If final receiver intervals `lower_j <= r_after_j <= upper_j` are proposed,
a necessary condition is

```text
sum(z_j*lower_j) <= B-d <= sum(z_j*upper_j).
```

For change bounds `abs(delta_r_j)<=epsilon_j`, the necessary condition is
`abs(d)<=sum(z_j*epsilon_j)`. Passing either aggregate test is **not sufficient**:
individual face capacities, subsets, shared receivers and final wind bounds can
still obstruct a solution. No physical epsilon, interval or covariance is inferred
from these diagnostics.

For the complete fixed receiver union:

| Quantity | Actual value |
|---|---:|
| `d` | +130654395.82388632 |
| `B` | -7844288.881723809 |
| `W` | 2032213967517.0898 |
| Necessary maximum change | 6.429165330e-5 s^-1 |
| Necessary maximum final absolute residual | 6.815162523e-5 s^-1 |

Consequently exact active closure cannot coexist with unchanged receiver
continuity, nor with zero final receiver continuity, on this fixed face set.
Changing the optimizer cannot remove this obstruction. These are bounds for the
**exact target**, not claims that a float32 candidate satisfies an exact equation.
For nonzero active residual `e_P`, replace `B-d` by `B-d-z_P^T e_P`.
With certified active limits `abs(e_i)<=t_i`, enlarge the allowable weighted
interval by `sum(z_i*t_i)` before using it to reject a tolerance-based problem.

The replay separately checks the actual stored signed changes; their active and
receiver weighted sums cancel within float64 aggregation roundoff. The current
row-wise `math.fsum` reports 0; an earlier face-wise aggregation reported about
`-4.47e-8`, not an exact-arithmetic conservation certificate. It does not project away the
stored active residual or edit b to force the exact-target identities.

## Shared receivers couple the old components

Construct a bipartite graph between the original active components and their
incident receiver rows. A receiver is represented once. The 8 old components form
**7 coupled groups**: the 722,376-row component and the 453-row component share
receivers; the other components remain separate. Their receiver capacity must
not be counted independently twice.

| Active rows in coupled group | Unique receiver rows | Necessary maximum change (s^-1) | Necessary maximum final residual (s^-1) |
|---:|---:|---:|---:|
| 722,829 (722,376 + 453) | 78,889 | 6.485013471e-5 | 6.876836085e-5 |
| 670 | 336 | 4.871959608e-5 | 5.450341959e-5 |
| 135 | 87 | 8.934243987e-6 | 2.107680129e-5 |
| 1 | 1 | 1.840138552e-5 | 8.773767530e-6 |
| 1 | 1 | 2.503677123e-5 | 3.164727260e-5 |
| 1 | 1 | 1.761139852e-5 | 1.528784752e-5 |
| 1 | 1 | 1.391591027e-5 | 9.804092705e-6 |

The strongest group bound exceeds the global bound. These groups are an audit of
coupling through hypothetical controls; they do not redefine production A or
certify a newly expanded operator's nullspace. If the domain or permitted faces
change, rebuild that operator, graph and certificates.

The PR #29 maximum-flow network represented all outside receivers by one reservoir.
It therefore did not enforce individual receiver balances or final intervals.
A receiver-aware network would need distinct receiver nodes and their limits;
merely imposing a limit on that one reservoir cannot express this joint problem.

## PR30 follow-up: cancellation and receiver non-worsening

The diagnostic arithmetic check now uses the uncancelled six-face magnitude
`S(y)=(|u_E|+|u_W|)/dx+(|v_N|+|v_S|)/dy+(|omega_L|+|omega_U|)/dp`.
Its budget tolerance is `256*eps64*sum(z*(S(before)+S(after)))` over P and R.
The previous expression used already-cancelled divergence and could shrink to
order eps64 squared for an exact internal circulation. `math.fsum` cannot undo
rounding inside each divergence. This change concerns arithmetic closure only;
it does not relax production or physical residual limits. The factor 256 is a
conservative diagnostic allowance, not a universal interval-arithmetic proof.

Replaying the same snapshot, row export and stored candidate gives closure 0 and
an arithmetic allowance of **0.011127543673908715** in the discrete weighted units.
The receiver states, statistics and original lower bounds are unchanged.

A stronger proposed receiver restriction is `|r_after_j|<=|r_before_j|` for every
receiver. This differs from preserving signed residuals. For an exact active goal
its necessary aggregate condition is

```text
C = sum(z_R*abs(r_before))
margin = C - abs(B-d) >= 0.
```

The new report records this margin without claiming sufficiency or physical
permission. Direct evaluation on the same actual arrays gives:

| Active components | Receiver rows | C | margin |
|---|---:|---:|---:|
| 0 + 7 | 78,889 | 67,284,801.91907342 | -71,721,985.5731432 |
| 1 | 1 | 246.7787663974 | +21.8862197772 |
| 2 | 1 | 170.2705218608 | -644.8866519677 |
| 3 | 1 | 59.8539478276 | -333.9562267363 |
| 4 | 1 | 611.0693773715 | +358.4985479868 |
| 5 | 87 | 59,309.36590895 | +11,861.30841523 |
| 6 | 336 | 178,948.81097866 | -283,392.34885851 |
| Whole union | 79,316 | 67,524,148.06857449 | -70,974,536.63703564 |

Thus four of seven groups already violate a necessary condition. On this fixed
hypothetical face set, **exact active closure and receiver-wise non-worsening are
jointly infeasible**, even before face capacity and total increment limits are
imposed. An optimizer cannot repair this failed sum constraint. Positive margins
in the other three groups do not prove their bounded feasibility. These are
float64 diagnostic evaluations with large obstruction margins, not a new exact
rational certificate for the full dataset.

For a tolerance-based active goal, use `C+sum(z_P*t_P)` instead of C; that allowance
must be established independently. The actual stored candidate has
`z_P^T e_P=0.047542259208776264` globally, far smaller than the global shortfall,
but its observed error is not a bound on every possible candidate. No target or
receiver tolerance is chosen from the shortfall. The next scientific decision
must identify a justified change to the active target, authorized faces, receiver
intervals or external tendency; production rejection remains in force until then.

## Implementation decision and remaining physical contract

The next production problem must state actual authorized faces, units, uncertainty
cost, active targets and **final** receiver intervals, then check joint feasibility
under total stored increments. The current conservative horizontal gate can be
expressed with `abs(delta_U)<=m_u`, `abs(delta_V)<=m_v`,
`m_u^2+m_v^2<=100`; the 7/7 box is only an inner approximation. Clipping an
unconstrained solution afterward does not solve this constrained problem.

The present evidence rules out “preserve every receiver” and “make every receiver
zero” under the current hypothetical closed face set. The non-worsening restriction also fails for an exact active goal, as shown above.
It does not identify which physical boundary, mass tendency or observations authorize a different problem.
Such a policy cannot be derived from beta, a 5 Pa/s cap, or solver convergence.
No unauthorized boundary/source adjustment is added to production. Receiver-aware
joint optimization and native mass/thermodynamic/forecast validation remain OPEN.


## Reproduction and validation artifacts

`tests/diagnose_qbal_receivers.py` reconstructs the active graph, hypothetical
pure-support omega controls, immutable-face masks, fixed receiver union and
coupled groups from the snapshot and sparse export. It rejects changed sentinel
masks, non-float32 or nonfinite candidates, changes outside the hypothetical set,
and an RHS inconsistent with the original divergence. It writes a JSON summary
and an NPZ containing all receiver before/change/after values, weights, group
labels, face coordinates and masks. The masks state a diagnostic assumption,
not production permission.

The exact-path packager `tests/package_qbal_feasibility.py` takes a JSON mapping
from each bundle-relative destination to its source and SHA256. It verifies bytes
both before and after copying and refuses an existing output directory. The
replay script is installed in the bundle as `scripts/replay_receivers.sh`:

```sh
python3 tests/package_qbal_feasibility.py --spec bundle_spec.json --bundle receiver_bundle
bash receiver_bundle/scripts/replay_receivers.sh
```

The replay verifies all input hashes, recomputes the report, and compares every
receiver/face array with the bundled reference. It also compares all reconstructed
control indices, directions, stored-face and receiver coordinates with the original
`root_omega_controls.npz` covered by the historical receipt. NPZ ZIP metadata is not used as
an array equality test. NumPy/SciPy are diagnostic dependencies only; no new
production dependency is added.

The local concrete bundle is `scratch/pr30_receiver_bundle_final/` in the isolated
PR30 worktree. Its 28 artifact manifest includes the actual snapshot, row export,
stored candidate NPZ/binary, explicit hypothetical masks, receiver arrays,
production Fortran source, current replay scripts, historical candidate-generation
scripts, and Intel replay driver/oracle/logs. `scripts/historical/` and
`scripts/intel_replay/` retain their original path assumptions. The portable
receiver replay runs from bundled paths; it does not regenerate the historical
AMG/max-flow candidate or claim the historical scripts are portable.

The stored binary and NPZ U/V/omega fields were compared bitwise and match. All
40 artifacts in the PR29 numerical receipt retain their original hashes. The
new bundle replay reproduces the complete report and every row/face array.
Current evidence is under `scratch/pr30_validation/`; the original PR29 evidence
is preserved in its own worktree. No synthetic test is substituted for this
actual 235×283×22 snapshot.

The PR30 follow-up evidence is in the isolated PR31 worktree under
`scratch/pr31_validation/`. It re-evaluates the actual stored candidate, not a new
BALCON or forecast run. The pinned Intel O0/O2 comparison above remains historical
PR30 evidence; this diagnostic-only change does not rerun or modify production
Fortran. The portable replay references the updated diagnostic and report while
retaining the original input and candidate hashes.

The follow-up regression constructs three binary-exact internal cycles on eight
active rows and four unchanged receivers. A rational oracle gives zero divergence
on all twelve rows. Float64 weighted closure is `-1.6940658945086007e-13`; the old
cancelled-residual scale rejects it, while the six-face allowance accepts it.
Injecting an incorrect `1e-3 s^-1` active divergence still rejects the budget.
All 39 Q-BAL diagnostic tests pass, including the 18 receiver tests. These small
algebra tests do not replace the actual snapshot replay.
