# CP02 completion worklog — 2026-09-10

The active user goal is full CP02 completion, including independent GREEN and
RED audits. Original E01/E02/E03/E05/E06 conditions remain the exit criteria;
individual build or replay successes do not replace them. CP02 remains
**IN_PROGRESS / NOT_RUN**. Work is confined to isolated engineering paths.

## Current evidence and work

Latest update — 2026-09-14: The [model-dynamics experiment](CP02_MODEL_DYNAMICS_INCREMENT_EXPERIMENT_20260914.md)
now has accepted actual pressure-analysis O0/O2 results and unchanged-scalar
WPS/metgrid/real comparisons. Native W transfer v1 remains rejected at the
near-terrain support boundary. Source-bound surface reconstruction v2 now passes
actual Intel consumption and corrected independent readback under its declared
fixed-geometry contract. These results do not close #4/E03 or
replace the original coupled validation requirements.

The previous maintained matrix has six pinned-Intel builds and32 supervised
calls passing,112 exact original products, eight128-variable OFF comparisons
and eight original WPS comparisons. Its immutable index remains
`scratch/cp02_link_input_pins_qj349ens/MAINTAINED_MATRIX_FINAL_RECEIPT.json`.
A newer compiler-process audit now passes all six fresh derived/balance/LAPSPREP
O0/O2 builds: discovery pins external regular input bytes before a separate
accepted build; final trace verifies the same path set and bytes. Exact index:
`scratch/cp02_compiler_process_4t9f9hse/AUDITED_BUILDS.json`.
The new32 supervised calls now pass, with112 original products byte-identical,
unchanged inputs, ECHILD termination, eight128-variable comparisons and eight
original WPS matches. Eight fresh profile figures have exact input/source/runtime/
output pins checked. Parent final receipt:
`scratch/cp02_compiler_process_4t9f9hse/MATRIX_FINAL_RECEIPT.json`, SHA256
`57e3c2511d54a128c44c2fff6fcde1cf374bb9eceafdbf8947d186f31ff193f3`.
Independent matrix GREEN now passes1072 checks; V4 publication integration
remains in progress.
The current41-file portable registry passes on supported private scratch with
no source drift: `scratch/cp02_final_portable41_ewyxj9vt/RECEIPT.json`.
The final run binds the frozen E01 and mandatory-compiler E06 sources, includes
exact source snapshots and has no drift. Actual E06 protocol/audits are separate. The previous39-file receipt remains historical.

E06 V3 closes all three R2 path/digest/helper attribution defects. Independent
RED re-executed both41-check suites and verified eight generation manifests:
`scratch/cp02_exit_red.e06_v3_review_20260910.fWJbyD/CP02_E06_V3_RED_FOLLOWUP.md`.
V3 final index is
`scratch/cp02_e06_path_binding_20260910/FINAL_INDEX_PENDING_RED.json`;
its filename is preserved despite the later independent RED result. This scoped
PASS uses the previous maintained matrix. New compiler-audit integration is
separate; publisher-produced recovery trust boundaries remain explicit.

E01 four positive gates and the final causal/absent-executable evidence now
pass independent GREEN/RED within their exact indexed scopes. RED confirms
closure of fault-causality and caller-controlled predecessor defects; earlier
positive records retain their historical source bindings. CP06-A actual target/support/sigma/threshold authority and
coupled native round-trip remain open. The structural graph now has4782 nodes/10046 edges/307 communities with316
source hashes unchanged across refresh; prior configuration IDs/edges are
preserved. Exact artifacts and historical failures are retained below.

The following OFF correction history used partial inputs that omitted LGT.
Its results remain scoped to those exact older inputs.

**OFF evidence correction:** the retained 235-record research outputs are
O0/O2-reproducible, but are not original cold-start OFF equivalents. A fresh
original-only LAPSPREP baseline has 108 records. The prior OFF path changes
8 HGT slabs through a canonical float32 round-trip and adds nonzero condensates
despite HOTSTART=false. These original-comparison findings supersede any broader
OFF-equivalence interpretation of the historical replay checks below. The
artifacts and their scoped reproducibility results remain preserved. See
`scratch/cp02_original_lapsprep12_9ibxh9x0/BASELINE_REPORT.md` and
`scratch/cp02_off_species_scope_zfm5z8nr/OFF_SPECIES_SCOPE.md`. The correction
now passes the bounded 12 UTC actual-binary regression: ordinary/OFF × QV
on/off × O0/O2 gives eight successful runs; ten malformed-input runs reject
without output. All 108 original records are whole-file byte-identical; the
129-record option adds QV only. Canonical OFF execution/equality is checked
before returning without assigning mapped fields to the host. The portable
LAPSPREP vapor suite passes. Owner evidence:
`scratch/cp02_off_host_fix_lq3h4q6z/OFF_HOST_FIX_RECEIPT.json`
(SHA256 `e76591c0112f079ebbb23427ed35449ffbbfa2826420ec54d86a8c775e69c547`).
Parent verified implementation/build/profile pins and the actual-regression
receipt digest. The final test-helper delta and 13–15 UTC extension now have a separate
`FINAL_DELTA_RECEIPT.json` (SHA256
`ea2196e53d985a461bace73a54b6e78c0f0ad27cb4c935355d4f8727042621d5`).
The extension passes 30 runs: six original O0/O2 runs and 24 ordinary/OFF QV
combinations. Parent independently checked all 16 corrected OFF file hashes,
all original baselines, 108-record whole-file equality, and final source pins.
12 UTC compares maintained O0/O2 to original O0; original O2 was separately
executed for 13–15 UTC. Independent GREEN and RED audits now both pass the bounded four-case cold
OFF host-preservation contract. GREEN completed 901 read-only checks; RED
found no bounded blocker. Reports are
`scratch/cp02_off_host_fix_green.RB0Aas/GREEN_OFF_AUDIT.md` and
`scratch/cp02_cloud_red_off_audit_20260910_v2/RED_REPORT.md`; parent report
hashes are in `scratch/cp02_completion_ui53t82f/off_independent_audits.json`.
The authoritative portable passing log is `vapor_suite_verified.log`;
`vapor_suite_final.log` preserves an incomplete failed attempt. External
Landlock helper lineage is not fully sealed by this evidence. Full native/OFF
closure remains unproven.

| Requirement | Current evidence | Required continuation |
|---|---|---|
| E01 | Four actual-LGT original six-stage runs,24 producer/4 wrapper exits0,104 NetCDF checks, original input preservation; VRT same-byte gate fixed | Complete executable cycle/freshness/coverage and required negative gates; retain101/104 whole-file repeat with three original LSX QC-comment differences |
| E02 | Six current maintained O0/O2 full builds;32 supervised calls,112 original products,8×128-variable payload/WPS equality; GREEN PASS_SCOPED | Complete concrete full dependency audit; link inputs are now pinned before final link; operational deployment is outside CP02 |
| E03 | Four actual-LGT original O0/O2 baselines, ordinary/OFF32-positive/10-negative matrix and current payload/WPS equality | Full OFF and coupled canonical-to-native comparison after CP06-A; no actual target/support/threshold authority inferred |
| E05 | Unsupported Lustre rejection; owned overlay capability and four-case post-RED publication/failure/retry/recovery PASS | Final independent review and latest eight-chain protocol; no physical power-loss or operational durability claim |
| E06 | Self-contained older-matrix four-case bundles, input/build/run/validator/figure objects; real publisher semantic dispatch and baseline PASS fixes | Independent RED final review; integrate latest eight-chain prospective link pins and exact new figures; preserve historical/runtime-closure limits |

## Build and OFF evidence

Parent evidence directory: `scratch/cp02_completion_ui53t82f/`.

`build_receipt.json` records five clean pinned-ifx executable links:

- balance O0: `scratch/upstream_balance_build.CSeh93`
- balance O2: `scratch/upstream_balance_build.nAVMBd`
- derived O2: `scratch/upstream_derived_build.encW96`
- LAPSPREP O0: `scratch/upstream_lapsprep_build.O9E7eK`
- LAPSPREP O2: `scratch/upstream_lapsprep_build.xBXvCO`

`tools/build_upstream_producer.sh` now accepts an optional `O0|O2` argument;
its default remains O0. Invalid optimization values reject before compilation.
Each build uses a fresh scratch directory, the pinned Intel profile, and records
its selected flags. The previous builder bytes are retained in
`build_upstream_producer.before.sh`; previous source-bound receipts retain their
historical meaning and are not certificates for the changed builder.

`fresh_off_receipt.json` and `run_fresh_off.sh` record two actual OFF runs through
the generated verified launcher and Landlock ABI 4. Both exit 0, preserve all
121 declared input files and omit the SHADOW sidecar as required for OFF.
Both full WPS files, retained under `off_outputs/`, have SHA256
`3501a1348d00d2ea1c8549600f03e4dbd9540d59cba55dee91d1ad89d1b88f3b`,
identical to the historical OFF. This is actual LAPSPREP reproduction, not a new
metgrid/real run, complete read tracing or coupled native acceptance.

`retained_native_identity.json` captures 36 current retained artifacts and
resolves the historical OFF WPS connection. It cannot retroactively prove
which native binary/input bytes a historical process consumed.

## CP06-A dependency: recoverable radar metadata loss

The pressure experiment currently retained is a numerical thermo/geometry
research result; it does not establish an actual nonzero observational dynamic
target. CP06-A requires its own case/input/support/target/threshold evidence.

`velocity_input_audit.json` independently verifies the pinned 40 gridded radar
input hashes. There are zero raw Nyquist values in the existing audit's
positive, below-200 m/s acceptance interval, and 121,848 nonfinite raw values.
This is not merely an erroneous NetCDF valid-range mask.

However, original 13 UTC FQC inputs exist. `raw_nyquist_audit.json` binds all
11 files and their sweep-indexed Nyquist values. The original decoder reads
`nyquist_velocity(sweep)` into a ray-sized array, then selects
`nyquist(ik*nrays(ik))`. The pinned-ifx diagnostic `nyquist_probe.f90` demonstrates
that those selected elements are outside the portion populated from the file.
The sentinel in this diagnostic is instrumentation, not an observed raw value.

A scratch-only decoder experiment corrects the sweep indexing and additional
malformed NetCDF attribute calls that caused a SIGSEGV in the first pinned-ifx
attempt. `decoder_probe_verification.json` records successful isolated GSN
decoding with all nine output Nyquist values exactly matching the raw sweeps.
Failed-attempt logs and both experimental sources are retained. This does not
validate the remaining decoder packing, range, ray layout, time, de-aliasing,
fall-speed or wind-frame semantics, and supplies no wind-target authority.
Further isolated decoder work is underway; operational sources are untouched.

## Independent review

`scratch/cp02_exit_red.1kSJYq/CP02_EXIT_RED.md` checks the original exit conditions.
Its operational Intel audit returns BLOCKED with 38 findings. Missing canonical
stage integration remains an E02 gap. The raw radar data recovery is actionable
work rather than evidence that CP06-A is complete.

Metadata staging now reads and hashes the same immutable byte buffers, pins the
canonical static grid and ncgen, and publishes with atomic no-replace rename.
Its nine tests pass. Writer-lifetime correction now uses a separate coordinator
snapshot, leaves producer paths untouched, and rejects FIFO inputs without
blocking. Both changes are undergoing independent final RED review.

The complete registered Python contract runner passes after these corrections;
`python_contracts.final.log` retains the output. This does not imply physical
validation of a snapshot or supported durable publication on Lustre. No CP02 exit, native/science
acceptance, or operational publication is claimed by this worklog.


## Fresh four-case chain and OFF continuation

The ordered producer evidence is retained separately for every original case:

- 12 UTC: `scratch/cp02_traced12_hayuckcb/`
- 13 UTC: `scratch/cp02_traced13_9qidm_8b/`
- 14 UTC: `scratch/cp02_traced14_0m57wu0f/`
- 15 UTC: `scratch/cp02_traced15_kr1obe6b/`

Each case starts with empty generated-product directories and preserves its
initial staged input snapshot, exact executed orchestration, six verified
launcher logs, selected-syscall traces, and final products. All 24 stages exit
zero. Missing prior LSX/LM2 and declared absent observations retain explicit
fallback scope. The staged original CDL metadata has not yet been replaced by
the metadata correction; stale valid ranges and PBL navigation remain pending.

`four_case_direct_content.json` records eight successful LT1/LSX content checks.
`four_case_lq3_lsx_coverage.json` records 100% SH coverage above ground in every
case using the same-run LSX surface pressure. The separate FSF-mask inventory
uses a different reference surface and is not a canonical coverage verdict.
LCO COM is sparse cloud vertical motion in Pa/s; it is not condensate and does
not establish an observational coupled wind target.

`trace_parser_summary.json` resolves all observed descriptors in 24 selected
syscall traces. This is not exhaustive I/O tracing. The classification receipt
identifies 55 external regular files, including dynamically loaded Intel
libraries; their after-execution hashes are not execution-bound identities.
The initial input versus generated-product distinction is recorded, with no
unresolved observed path. Stronger runtime closure remains open.

`four_case_off_receipt.json` records eight subsequent actual LAPSPREP OFF runs
with the pinned O0/O2 builds. Input inventories (112–114 files per case) remain
unchanged, and private outputs contain only the WPS file. Per-case O0/O2 files
are byte-identical. `four_case_off_content.json` and each output's `content.json`
record exact 235-record inventories, valid time, units, dimensions, finite
non-fill values, nonnegative water, projection and grid-relative wind flags.
These runs do not establish a coupled candidate, full native acceptance, or
canonical adapter integration. Full metgrid/real expansion still follows
CP06-A under the original checkpoint scope.


## Intel audit symbol correction

The Intel integration audit incorrectly required GNU-style `__module_MOD_entry`
names. Fresh pinned-ifx compilation in `/tmp` demonstrates Intel's
`module_mp_entry_` names for all three declared stage entries. The audit now
requires those exact global definitions; GNU-shaped and local/fake symbols do
not satisfy it. The focused audit suite passes 22 tests, and
`ifx_entry_symbol_receipt.json` retains the three real compiler probes.
The probe modules are only naming diagnostics, not implementations of the
missing stage adapters. The operational integration blocker remains open.


## Follow-up RED and direct-field evidence

The final metadata RED report at
`scratch/cp02_metadata_red_final_20260910_r1/CP02_METADATA_STAGE_FINAL_RED.md`
confirms the exact-byte, canonical-grid, no-replace, ncgen and preservation
corrections. It still reproduces a blocking FIFO open and an unnormalized error
when the output parent is a regular file. Both are assigned for correction;
metadata staging is not yet declared fully reviewed. Lustre `EINVAL` remains a
safely rejected unsupported backend, not permission to overwrite.

The four-case OFF content check additionally confirms exact same-run LSX PSFC,
exact LT1 temperature above ground, and exact float32 `SH/(1-SH)` vapor mixing
ratio above ground. The checked per-case cell counts are 1,294,674; 1,294,999;
1,295,316; and 1,295,476. These checks independently tie the WPS values to the
fresh producer data and the declared dry-air vapor denominator.

After the compiler-symbol correction, `intel_audit_corrected.json` still reports
38 operational integration blockers. The correction does not waive any source,
runtime, ABI, job-order or adapter requirement.


## Latest review handoff

Independent GREEN has completed the original traced four-case review:
`four_case_green_verification.md` and `.json` confirm all 104 NetCDF products,
24 stage transitions, VRT gates, LSX-based SH coverage and the selected trace
inventories. Its PBL navigation and surface-log cycle-marker limitations remain
explicit. This review precedes the corrected-CDL replay now running under
`scratch/cp02_metadata_four_cases_ftkd0kb4/`.

The final2 metadata RED review
(`scratch/cp02_metadata_red_final2_20260910_r1/CP02_METADATA_STAGE_FINAL2_RED.md`)
confirms both remaining error-path corrections and 11 passing tests. It also
independently confirms the ifx audit naming correction and 22 audit tests.

The transaction final RED review at
`scratch/cp02_writer_lifetime_final_red.iXLyUm/RED_REVIEW.md` passes the scoped
retained-writer/FIFO/source-race corrections but identifies a real E06 caller
integration defect: semantic validators read staging before detachment, and the
post-commit verifier runs too late to reject publication. The coordinator and
science runners are being changed to validate the exact detached snapshot before
publication. Arbitrary same-UID attacks on the coordinator publication namespace
remain outside the already documented trust model; extra pathname checks cannot
establish isolation from such an actor.

The isolated radar decoder reports 11 raw files, 89 sweeps and 32,040 rays, with
56 explicitly clipped sweeps, in
`scratch/cp02_radar_decoder_final_20260910/receipt.json`. Independent decoder RED
review is underway; this remains raw-moment recovery, without target authority.
A pinned-ifx scratch link of the original downstream remapper also succeeds.
Actual GSN runs expose a missing integer function declaration and then an
invalid Nyquist/missing-data propagation path causing floating overflow. Their
logs are retained under `scratch/cp02_remap_gsn_*`; no remap product or science
acceptance is claimed. Isolated consumer repair is continuing without disabling
floating-point traps or changing operational sources.


## Corrected metadata replay and newly reproduced wind defect

All 24 corrected-CDL producer stages completed, with 26 NetCDF products plus
LPBL per case. The agent's comparison in
`scratch/cp02_metadata_four_cases_ftkd0kb4/FINAL_METADATA_INTEGRATION_REPORT.md`
confirms only five initial CDL files differ, all eight intended stale ranges are
removed and all eleven PBL navigation values are populated. Physical arrays in
the five metadata-target products remain identical. However, 27 variables in
six wind-lineage products differ in every case; raw reproduction is **FAIL**.
The changed CDL files are absent from the observed wind read inventory.

Two more fresh same-input 38-thread wind runs reproduce the failure (u/v maximum
differences about 2.34/2.33 m/s). This is not attributed to ordinary roundoff.
The original Barnes observation-bound loop assigns shared `i,j,k` inside a
parallel region without private declarations. An isolated copy at
`src/upstream/wind_openmp/barnes_multivariate.f90` changes only that loop to
`PRIVATE(i,j,k)`. A new pinned-ifx diagnostic object and full executable link
reuse the previously verified unchanged objects; this is not a new all-source
build. Actual fixed 38-thread repeats and original serial controls are running.
No operational source or existing verified builder was changed.

The corrected-CDL outputs also passed eight new LAPSPREP OFF O0/O2 runs and the
same 235-record/PSFC/TT/QV direct checks. Receipts are under
`scratch/cp02_completion_ui53t82f/metadata_off_continuation/`. Per-case O0/O2 bytes
match, but output hashes differ from the earlier wind-affected replay. These
I/O checks neither erase the wind reproduction failure nor establish a
metadata-only whole-chain equivalence claim.


The two actual corrected 38-thread wind runs now both exit zero and have
identical initial manifests and all 39 retained `lapsprd` files, including
LW3/LWM and generated diagnostics. `wind_fixed_repeat_comparison.json` records
that bounded result, and `wind_private_indices_identity.json` binds the isolated
one-directive source correction. Independent source review agrees that shared
observation-index temporaries caused a race. Original serial controls are still
running. Full fresh builder integration and four-case regressions are pending;
no earlier nonreproducible result is relabeled as PASS.

The decoder RED review additionally found an incorrect time origin: an IIA raw
file describes 22 UTC while the common runner uses 13 UTC, and its validator
repeats the same erroneous epoch-plus-offset assumption. Time-origin validation,
input binding and strict scratch working-directory compilation are being fixed
before the raw decoder is accepted as time-authoritative.


## Fresh full-build integration

The original O0 serial wind control ended at its 600-second limit with exit 124;
no LW3/LWM was completed and no serial equivalence is claimed. Its queued second
run was not started. The agent sealed the old source/build postchecks before
releasing the builder for modification.

The maintained builder now selects the isolated Barnes source alongside the
existing isolated wind main/subroutine. The exact previous builder bytes remain
at `build_upstream_producer.pre_wind_fix.sh`; old receipts retain historical
meaning and are not current-builder certificates. A two-process fresh-build
batch under `fixed_full_builds/` starts with O0/O2 wind, then rebuilds all six
ordered producers, balance and LAPSPREP, including O2 LAPSPREP. Every build uses
the pinned Intel profile and its own scratch working directory. Independent RED
review of the wind correction is in progress. New full-chain regression remains
required before closing the wind reproduction defect.


All ten fresh full links have now passed. `fixed_full_builds/verified_receipt.json`
independently verifies every declared source/runtime/executable hash and confirms
the corrected Barnes object is selected in both wind link maps. The first new
full-chain case (12 UTC) completes all six producers; its LW3/LWM bytes exactly
match the earlier corrected diagnostic runs, as recorded in
`fixed_full_build_wind12_comparison.json`. The four-case replay and a fresh O2
single-thread versus 38-thread comparison remain in progress.


## Wind serial reference and metadata control completed

The fresh O2 wind serial and 38-thread runs both exited zero with identical
initial inputs and all 39 retained `lapsprd` files byte-identical.
`wind_o2_thread_comparison.json` records this PASS; the earlier O0 serial timeout
remains a separate failed control. All four core-fix full-chain cases completed
their 24 producer stages. The same fixed build with original versus corrected
CDL at 12 UTC has identical physical values: only eight obsolete `valid_range`
attributes and eleven intended PBL navigation fields differ across 66 files.
See `fixed_metadata_control_comparison.json` and `verify_fixed_replays.py`.

Independent wind RED also found an unsynchronized error-path diagnostic. The
final isolated source adds a named OpenMP critical section around that write,
preserving the original invalid-time status and weight policy. Focused actual
routine O0/O2 tests with 38 threads passed 4,096 calls and 2,048 complete invalid
time diagnostics per test. Fresh full O0/O2 wind links and normal 12 UTC runs
match all 39 corrected baseline files. The receipt is
`scratch/cp02_wind_log_final_56a4kvjl/FINAL_WIND_LOGGING_RECEIPT.json`.
A new final four-case and OFF regression is running under
`scratch/cp02_final_logging_cases_n74wlcn1/`; prior evidence is preserved.

E06 RED verifies required prepare/receipt handling, retained-writer isolation,
snapshot mutation rejection and fourteen validated-path fsync boundaries. It
also identifies missing trusted validator identity binding and no actual full
Fortran science-runner execution. Identity binding is being fixed. The two real
science runners now compile from their fresh scratch working directories and
execute the driver bytes sealed against their recorded build digest. Their
clean-source guard remains intact; actual integration will use a separately
committed private source snapshot, not misidentify the dirty workspace as HEAD.

The radar consumer RED identifies swallowed failures and insufficient input
schema/geometry checks. Decoder time-origin corrections and consumer fail-closed
remediation remain underway. These results do not establish observed target
authority or close CP02's full adapter/native/runtime requirements.


The final logging-source four-case regression has now completed: 24/24 stages,
104 NetCDF products plus four LPBL files, and all other retained products match
the corresponding corrected baseline byte-for-byte. All eight actual OFF runs
pass, preserve inputs, contain 235 required WPS records, and have per-case O0/O2
byte equality with direct PSFC/TT/QV checks. All eight producer/LAPSPREP build
manifest sets and source pins pass postchecks. Authoritative evidence:
`scratch/cp02_final_logging_cases_n74wlcn1/FINAL_LOGGING_CASES_OFF_RECEIPT.json`,
SHA256 `906041d338e0daaccebea4ba9b39e807be653fbc11f31e9e66126218617074bd`.
Independent final GREEN inspection is underway. This replay does not supply
actual Cloud-BAL adapter integration or native acceptance. The E06 science
callers separately retain their reviewed historical prepared-input manifests.


## Actual E06 physical-runner publication

Both maintained pinned-ifx science runners now complete real Fortran execution,
semantic validation of the detached snapshot, and publication with required
validation receipts. Four manufactured real-geometry cases yield 17 products;
four radar-only engineering SHADOW cases yield 13 products. Both processes exit
zero. Exact source is private clean commit
`255e456310480c78f788ab978058f352d5b2abc1`, copied from recorded workspace bytes;
it is not the original workspace HEAD. Evidence is under
`scratch/cp02_completion_ui53t82f/e06_real_execution_6nt5rqm8/`.
`execution_receipt.json` verifies the private repository remains clean and every
copied source byte remains unchanged. `archive_receipt.json` binds a durable
source Git bundle, both complete committed generations, and fresh build
receipts/executables; the archive is evidence, not a publication root.

The first isolated attempt compiled successfully but stopped before producer
execution because `ldd` cannot inspect the noexec `/tmp` path directly. Its logs
and repository are retained under `e06_real_execution/`. The maintained callers
now pin the compiled driver before inspecting dependencies through the sealed
memfd using `tools/inspect_bound_runtime.py`. The existing execution-binding
implementation and upstream build manifests remain unchanged. The registered
runtime-inspection test passes dependency discovery and rejects a post-pin
executable mutation; an actual ifx driver also passes `ldd -r` through this path.

These are actual numerical/engineering execution and local tmpfs transaction
results using the two runners' historically pinned prepared inputs. They do not
claim that the new upstream generation is consumed by an actual Cloud-BAL
adapter, certify observation-derived dynamic targets, assess science/forecast
quality, or authorize operational publication. Final independent E06 RED is
being scheduled; full CP02 remains IN_PROGRESS/NOT_RUN.


Independent final GREEN now passes the complete final wind four-case/OFF
evidence: source/build/runtime/ELF pins, all 104 NetCDF plus four LPBL products,
selected trace inventories, 235-record WPS content and direct field mappings,
and same-case LSX-PS SH coverage with zero missing cells. Reports are
`scratch/cp02_final_logging_cases_n74wlcn1/GREEN_FINAL_LOGGING_AUDIT.{json,md}`;
sealed run receipts and the original evidence manifest remain unchanged.


The registered Python contract runner completes all 34 test files with exit
zero, including the new sealed runtime inspection regression. Log and receipt:
`python_contracts.current.log` and `python_contracts.current.receipt.json` under
the parent evidence directory. The E06 implementer separately records 25
recovery tests, ten workspace/snapshot verifier tests and other focused checks
in `scratch/cp02_writer_lifetime_e06_verify.vxDdIY/REVIEW.md`. Final E06 source
bytes still match the actual private execution capture; subsequent live-tree
deltas are unrelated radar consumer sources and this progress document.


## E06 post-exit RED correction

Final independent RED found that the SHADOW runner's successful exit still
activated its failure trap. That renamed the original staging directory after
publication, causing later `current` and `recover` to reject the publication
layout. Thus the earlier exit-zero SHADOW result is not a complete lifecycle
PASS; its source, process logs and archived content remain preserved evidence.
The manufactured caller already disarms the trap, and independent parent
post-process `current` and `recover` checks both pass for its earlier generation
(`manufactured_post_exit_receipt.json`).

The SHADOW caller now clears staging and removes the EXIT trap immediately
after successful commit. A new clean-snapshot actual SHADOW run is underway,
with `current` and `recover` tested by its parent only after the runner process
exits. Prior failed state is retained. RED report:
`scratch/cp02_writer_lifetime_e06_final_red.J3mj57/RED_REVIEW.md`.


The corrected SHADOW runner has now completed another actual four-case ifx run
with exit zero. Its parent independently invokes both `current` and `recover`
after the process has exited, and both return zero. Evidence:
`scratch/cp02_completion_ui53t82f/e06_real_execution_uybyq88o/`, clean private
source commit `e1b09699a6ea641a9f95626fc01662fc4048a005`. Its 13-product generation,
source bundle and build evidence are separately archived. The earlier MMS
execution remains valid at its own private commit; prior SHADOW post-exit
failure is preserved and is not reclassified. Final RED delta review is pending.

Final decoder RED passes the bounded raw decoding contract, including declared
CF origins, source/snapshot/build/runtime bindings, no-replace collision and six
retained negative/offset probes. IIA's 22 UTC is the input's declared origin,
not independent clock authority. Durable archive:
`scratch/cp02_radar_decoder_evidence_20260910/`, 154 verified archive entries and
the preserved original 190-entry RED manifest. Raw snapshots, final output and
probe evidence are retained with explicit original-to-archive path mapping.
Scalar coordinate type checks, decoder-side azimuth range checks, and canonical
output time metadata remain bounded caveats.

A parent consumer review identifies a real GSN range-origin mismatch: decoded
first gate is 125 m with 250 m spacing, while the legacy remap lookup table
computes the first gate at 250 m. Reading metadata into an otherwise unused
slant-range array does not fix that mapping. Consumer correction and direct
geometry-change probes remain required before its final acceptance.


Final E06 lifecycle RED passes the corrected SHADOW caller at clean private
commit `e1b09699a6ea641a9f95626fc01662fc4048a005`: process exit zero, independent
post-exit `current`/`recover` zero and `cloud_bal_current_evidence` PASS. The
28-file archive contains 13 verified products; archive receipt SHA256
`7d866ff9bac0efe0171fab1dac700fbf10fe8f79513d9bd2405250d8c7f639f7`.
The final driver SHA256 is
`117e00681427de7be8e196a822b64fa2a7aafbbd9c53e5c17004e53c3fe8af3f`.
Report: `scratch/cp02_writer_lifetime_e06_final_red_followup.mj1Gqc/RED_REVIEW.md`.
Previous failed SHADOW execution and its RED report remain preserved. This
closes the identified lifecycle defect within the trusted local publication
model; full CP02 integration and self-contained operational provenance remain
separate requirements.

## Direct-input inventory and final consumer review

A reproducible four-case, ten-role candidate QBAL inventory is retained at
`scratch/cp02_candidate_qbal_inventory_y_ailcpv/` (inventory SHA256
`9afaef87a687e83fdf07728bb1dd622da26a20302000d750d44602660936cd0b`).
It records exact products, source/build/stage evidence, time coordinates and
remaining per-role gaps. It is EVIDENCE_ONLY / NOT_READY / NO_AUTHORITY; no
complete generation manifest or canonical adapter acceptance is inferred.
No balance control was staged by the six-producer replay, and FSF background
pressure remains distinct from LSX analysis pressure.

Independent LCO RED reproduced two hash/read races: the index could be parsed
from different bytes than its accepted hash, and LCO content could be inspected
after replacement of the indexed product. Both retained four-case content and
lineage checks passed, but that does not close byte binding. Remediation must
hash and inspect the same immutable buffers, include separately named FSF and
LSX mask evidence, and separate portable fixtures from retained-case tests.
Original findings and probes are preserved at
`scratch/cp02_exit_red.lco_20260910_r1/CP02_LCO_COM_RED.md`.

The isolated remap owner reports a fresh pinned-ifx build at
`scratch/radar_remap_build.wZ4dZ9`, positive GSN execution at
`scratch/radar_remap_gsn_8jm7uo_f`, and successful negative probes. The actual
Fortran LUT byte probe returns gate indices [0, 0, 1] for the 125 m origin;
short-tilt gate counts are retained and padded gates masked. Final independent
RED review is pending. Legacy output-time binding remains provisional and this
evidence supplies no clock, dealiasing, fall-speed, QC or science-target authority.


## LCO portable registration, graph refresh and QBAL pilot failure

The LCO remediation now parses the exact hashed index buffer and inspects LCO,
FSF and LSX from detached in-memory NetCDF bytes. It reports separate background
and analysis-mask metrics without adding dynamic authority. The portable
fixture test is registered; retained four-case checks require `--real-evidence`.
The parent registered Python suite passes all 35 test files (exit zero), sealed
in `scratch/cp02_completion_ui53t82f/python_contracts.lco_registered.receipt.json`.
Checker SHA256: `abe9d5563f1b3bf6cbe961999b6dd13631188481b686f244fcf3b155ba419d2c`.
Independent final LCO RED review remains pending.

Graphify 0.9.53 local AST refresh completed: 3,984 -> 4,304 nodes, 8,344 ->
8,966 edges and 263 -> 282 communities. All 288 maintained files were byte
identical before/after extraction. The known CLI omission of unchanged config
records was repaired from the hash-verified prior snapshot: all 134 node
contents and 133 incident edges remain exact, with 14 derived community labels
changing during reclustering. No dangling endpoints, duplicates or self-loops
were found; document semantics were not re-extracted. Wiki report mirrors are
synchronized. Evidence: `scratch/cp02_completion_ui53t82f/graph_update/`, final
graph SHA256 `0c55545dc7d003383292f597dd2d8adef04ddbfff4a7044a4a55cc41f70ef66a`.

A private 12 UTC maintained legacy-derived QBAL pilot was attempted with the
exact original balance control, pinned existing ifx build, a declared 300-second
timeout and no changes to numerical gates or weights. It returned exit 1,
not a timeout: LEIBP3 returned failure in the first BALCON continuity solve.
No balanced outputs were created, no input was changed and LAPSPREP was not
started. The expected FUA read is confirmed by the selected trace. This is
failed execution evidence, not canonical adapter or science acceptance.
Evidence: `scratch/cp02_legacy_qbal_pilot12_tli9blvz/`; diagnosis is in progress.

The final positive remap LUTs are additionally preserved under
`scratch/cp02_completion_ui53t82f/remap_positive_lut_archive/`. The parent read
actual big-endian Fortran record bytes and independently verified the first
three zero-elevation range bins [0, 0, 1], versus [0, 1, 1] from the legacy
250 m first-gate formula. This is a bounded geometry oracle, not time or science
authority.


Independent remap RED passes the exact one-volume GSN result (5,086 matching
VEL/NYQ cells and the archived range oracle), but finds the caller still gates
LUT generation on `ntimes_radar == 1`. Therefore the broader per-volume claim
is not established; a caller correction and same-process second-volume probe
are now assigned. Geometry failures retain four private LUT intermediates;
"no output" means no accepted v04 product, not no diagnostic files. Report:
`scratch/cp02_radar_remap_followup_final_20260910/RED_REPORT.md`.

The initial QBAL failure is further narrowed: status 0 is an early guard
return, whereas iteration exhaustion would return status 2. It is not yet
justified to label this nonconvergence. The failed pilot is sealed in
`scratch/cp02_legacy_qbal_pilot12_tli9blvz/PILOT_FAILURE.md`; its evidence seal
SHA256 is `c9c2b8a5bcfce40d150ddd121d149fb9c0f0483d71166f3f09bb8b9889760a87`.
A separate scratch-source diagnostic build will log exact guard locations and
values without changing numerical equations, controls or acceptance thresholds.


Final independent LCO RED passes the bounded ingress at checker SHA256
`abe9d5563f1b3bf6cbe961999b6dd13631188481b686f244fcf3b155ba419d2c`.
Fresh replacement-after-read probes prove that the accepted original index and
LCO bytes are used despite on-disk replacement. Portable/explicit real-evidence,
dual FSF/LSX masks, default blocked/no-authority and path-preservation checks
pass. Ancillary receipt JSON is not a fully detached provenance DAG; this is
not whole E06/CP02 acceptance. Report:
`scratch/cp02_exit_red.lco_20260910_r2/CP02_LCO_COM_RED_FOLLOWUP.md`, SHA256
`edde275babf1985fb91ff14f654b3a268459821bb4da96385ce050af31a07c3c`.

Scratch-only QBAL instrumentation identifies the exact early return at inner
iteration 1, Fortran cell (109,35,4): diagonal -2.8969246e-21 and forcing
-5.9513164e-19 are finite and nonzero, but the absolute 1e-20 diagonal cutoff
classifies this positive compact-support row as degenerate. Influence is
1.7398687e-14; positive face coefficients remain represented. The diagnostic
input manifest matches the first failed pilot exactly. No numerical correction
has been applied and subsequent convergence is untested. Independent GREEN/RED
review is assessing bounded scale-aware handling before an implementation.
Evidence: `scratch/cp02_qbal_guard_diag12__5e806p3/DIAGNOSTIC_REPORT.md`;
diagnostic receipt SHA256
`49c49ced889756580229052c6ae7f5d3af4564f3846415bd3ce0e3cef69791b8`.


Independent numerical RED recommends exact-zero diagonal/forcing compatibility
for the observed representable row. The maintained source now applies that
minimal repair; finite checks, physical support, relaxation, convergence and
caller rollback remain in place. A truly zero row with any nonzero forcing
rejects. This does not establish robustness for arbitrary tiny support:
single-precision harmonic products can still underflow outside the observed
case and require separate stable/wider arithmetic before a general claim.

Fresh pinned Intel balance O0 full build passes at
`scratch/upstream_balance_build.UPuJfv`, source SHA256
`90c5a658bd77170a77de98cf9bc64b4f3d7c7a2b84c749f2aec206a447a86d4f` and executable
SHA256 `b73c78221e364a9d7f5d7933a81be5f6f914bf9c6753b1ee95f31f3c23c376de`.
Focused before/after tests and a new same-input actual pilot remain pending.
The prior qbal source bytes are preserved at
`scratch/cp02_completion_ui53t82f/qbalpe.pre_tiny_row_fix.f`.


Final GREEN exact-zero regression is retained at
`scratch/cp02_leibp3_guard_final2.SZ5Tvh/`: old O0/O2 exit 1, corrected O0/O2
exit 0. It compares elementwise one-iteration results for influence/forcing
scales 1 and 1e-14, zero/zero compatibility, rejection of disconnected 1e-30
and 2e-20 forcing, and active NaN rejection. It does not claim arbitrary
float32 harmonic robustness. Final test SHA256:
`89f0628392081185e622bcd2eeca3c3bd116f8266dafc9d33c86e228cf387dfe`.

The new actual 12 UTC pilot passes the corrected tiny-row guard but still
fails closed: LEIBP3 returns status 2 after its unchanged 200 sweeps (the
Fortran exit-loop print says 201). Maximum correction is about 336, above
`erf` about 51.33. The printed `corlm*cortm` residual diagnostic is not a global
residual norm. There are no products, input mutations or subsequent LAPSPREP
runs. No further parameter adjustment or retry was performed.
Report: `scratch/cp02_qbal_exactzero_pilot12_wpqg29uw/CORRECTED_PILOT_REPORT.md`;
receipt SHA256 `de08746bb0bb7839d6542f00e2967afe02352d601751b7560426378fa9dd50bc`.
This is the first demonstrated configured nonconvergence; the earlier status-0
failure was a distinct guard defect.

The second local AST refresh includes the remap caller and QBAL/test changes:
4,304 -> 4,309 nodes, 8,966 -> 8,979 edges, 282 -> 280 communities. All 289
maintained files stayed byte identical across extraction. Config node contents
(134) and incident edges (133) remain preserved, with 20 derived community
labels changing. Integrity and wiki mirror checks pass. Evidence:
`scratch/cp02_completion_ui53t82f/graph_volume_update/`, final graph SHA256
`e9d82f058a327033b9e88a3e60f25aff43516e4479f89619705099f168171df3`.


Final independent QBAL delta RED confirms the two-condition guard change,
focused before/after pinned O0/O2 evidence, unchanged caller rollback and
publication gates, and the actual status-2 pilot failure without outputs.
Report: `scratch/cp02_qbal_exactzero_delta_red.Ssh777/RED_REVIEW.md`.
The final focused receipt SHA256 is
`3393d807ab13e8285fc551472bd7f2bbf2ca34fe1427dded6cef2a2840da5d82`.
The bounded defect is corrected; full real-case convergence has failed under
the unchanged limits, and general harmonic-underflow robustness is not claimed.


The parent full pinned-Intel `tests/run_unit_tests.sh` run completed with exit
zero, including operator/rollback, canonical/transition, real SHADOW I/O and
upstream status regressions, and the final fixed-form 72-column QBAL compile.
Its 7,199-line log SHA256 is
`a24825567ce149ebde3b5cfb8ca2fb915121c72b8b62ce427c6d712e7dab3d69`;
receipt: `scratch/cp02_completion_ui53t82f/unit_tests.exactzero.receipt.json`.
This unit result does not override the separately failed real-data pilot.

The parent additionally compares actual retained one-/two-volume remap LUT
bytes: 23,816 entries differ between 125 m and 200 m first-gate geometries.
At elevation 2 degrees, gate 887, the range-bin formulas give 442.979984 and
443.129893, matching stored bins 442 and 443. The first three zero-elevation
bins are identical for these two origins and alone would not discriminate
regeneration; this nonzero-elevation oracle does. Evidence:
`scratch/cp02_completion_ui53t82f/remap_two_volume_parent_oracle.json`.


Final independent remap delta RED finds no source blocker within the requested
volume-regeneration correction. It independently verifies two volume completions,
125 -> 200 m geometry, 23,816 changed LUT entries, source/build/runtime/input
pins and fail-closed v04 statuses. The old independent audit bytes are preserved
separately; owner-updated reports were excluded from the independent verdict.
Report: `scratch/cp02_radar_remap_delta_red_followup_20260910/RED_REPORT.md`.
Provisional time, retained private diagnostic LUTs and absent science/dealias/QC
or full read-closure authority remain explicit.

The current remediation wave is indexed by
`scratch/cp02_completion_ui53t82f/CP02_REMEDIATION_WAVE_RECEIPT.json`.
All 289 maintained source/test/config files still match the final graph capture.
This receipt records bounded corrections and their passing tests alongside the
failed actual QBAL pilot. Whole CP02 remains IN_PROGRESS/NOT_RUN: actual
canonical adapter integration, complete provenance and the gated native
round-trip requirements are not closed by this wave.

## OFF correction structural refresh and detached loader discovery

The final OFF implementation and regression helper were stable during the local
AST refresh. `scratch/cp02_completion_ui53t82f/graph_off_update/` preserves the
before/after graph, source captures, delta, diagnostics and parent verification.
The graph changed from 4,309/8,979 to 4,318/9,000 nodes/edges (280 communities).
All 290 maintained source files were unchanged during extraction; the 134
unchanged configuration nodes and 133 incident edges retain exact content,
apart from derived community labels. Diagnostics report no missing/dangling
endpoints, duplicates or loops. Final graph SHA256:
`4b391e1200b760671f0656f0e4de390de741225978ba8d371ff1a4d9d5db8fa3`.
Wiki graph mirrors and the INDEX were synchronized; document semantics were
not re-extracted and no global/provider/schema configuration changed.

`scratch/cp02_detached_loader_probe_20260910/REPORT.md` records a discovery-only
experiment with the actual corrected O0 LAPSPREP ELF. The main ELF and loader
were separately hash-bound and sealed; 41 runtime objects were copied as
regular files with matching declared hashes. The loader's `--list` resolves
all ordinary libraries to those copies and reports itself by its sealed FD.
The initial strict receipt preserves that FD as an unexpected path rather
than silently broadening a pathname allowlist. This is not producer execution,
read confinement, constructor/dlopen evidence, or E02/E06 completion.

## Native lightning correction and runtime integration follow-up

The earlier four-case upstream and OFF evidence used a staging inventory that
omitted the existing native LGT observations. Those same-input preservation
results remain valid only for that partial inventory. The four actual LGT files
each contain 66,505 ordered records, including 1,032 native -99 missing cells
and positive observed counts. The maintained derived reader previously rejected
the entire partially covered file. It now obtains the native integer missing
marker from the existing configuration getter, maps only that marker to its
internal missing value, and preserves zero and positive counts. Invalid other
negative values retain the explicit missing-input fallback.

The extracted reader regression passes all 14 pinned-ifx O0/O2 cases. A fresh
maintained derived build and pure-original derived build produce all 14 outputs
byte-for-byte equally with identical actual-LGT input inventories. Seven outputs
(LCO, LCT, LIL, LMD, LRP, LTY and LWC) differ from the earlier omitted-LGT run.
This is a single-stage comparison using retained cloud/humidity inputs; the
LGT-inclusive cloud -> humidity -> derived sequence and downstream OFF comparison
still require regeneration. Evidence:
`scratch/cp02_lightning_native_missing_20260910/RECEIPT.json` and `REPORT.md`.
The original stack and missing-LGT FPE failures remain preserved.

Pure-original wind also completes at pinned-ifx O2, one thread, unlimited stack,
with 16/16 products byte-identical to the retained maintained 38-thread run and
no changed or removed inputs. The parent independently rehashed all 16 retained
products. Receipt: `scratch/cp02_original_e01_builds_20260910/wind_12_probe_stack_unlimited_O2/RECEIPT.json`.
The earlier O0 timeout and 8 MiB stack failures remain historical evidence;
this does not establish four-case original E01 closure.

The explicit Landlock read/write policy passed its focused tests and independent
RED follow-up after correcting two pre-existing hardlink alias escape classes.
Evidence: `scratch/cp02_landlock_policy_20260910/FINAL_RECEIPT.json` and
`scratch/cp02_cloud_red_landlock_final_k5foquzi/RED_REPORT.md`.
Its scope requires an exclusively owned namespace and closure of inherited file
descriptors; it does not itself bind bytes or exclude an unsandboxed writer.
A detached runtime helper has an actual original cold LAPSPREP execution and
post-exec library trace, but its static input directory was granted policy write
access for a legacy O_RDWR namelist probe. Mode 0400 alone does not establish
immutable input protection against same-owner chmod. That run is retained as
bounded compatibility evidence, pending a read-only policy rerun. Its input
inventory differs from the earlier final-13 baseline, so those WPS hashes are
not compared as same-input results.

The derived -> QBAL -> LAPSPREP state payload and adapters are still being
integrated. An actual full-grid export succeeds, but the current payload read
fails at field decoding; maintained caller cutover is pending correction.
The new coordinator passes an actual pinned-ifx launch smoke (declared input
read, undeclared read denial, output write), which is not an upstream stage run.
The ncgen child runtime closure remains separate work. The graph snapshot
4,318/9,000 is now stale for this source wave; no updated-source graph claim is
made. Full CP02 remains IN_PROGRESS/NOT_RUN with the original E01/E02/E03/E05/E06
and gated native requirements unchanged. LGT does not authorize a dynamic target.

## Actual-LGT original four-case regeneration and payload body milestone

Fresh pure-original builds have now completed all six producers for 12–15 UTC:
24/24 producer exits zero and 104 NetCDF products. Parent verification checks
linked inventories/order, immutable initial input hashes, all durable/live
product hashes, 235 by 283 grids, exact case clocks, raw finite values, and
records descriptive nonfill coverage. The first five stages use pinned-ifx O2,
derived uses O0, all one thread and unlimited stack. The mixed profile and
wrapper assertion failures on generated SDL/TMP rewrites are explicit; verified
continuations ran remaining stages only. The unavailable old pre-cloud LPBL
snapshot was not substituted: the full chain regenerates it from initial inputs.
Evidence: `scratch/cp02_original_e01_builds_20260910/ORIGINAL_LGT_FOUR_CASE_RECEIPT.json`
and adjacent report. Independent RED is pending. This supplies actual original
LGT-inclusive source regeneration evidence, not full runtime/read closure,
OFF/native equality, science acceptance or whole E01/CP02 closure.

The maintained replay declaration now includes exact native LGT hashes and
fixed-column/ordered-grid/native-missing content validation, with six focused
and 36 existing replay tests passing according to the owner. The previous
37-file portable Python suite also passed after registration of Landlock policy
and detached runtime tests; its exact source hashes are in
`scratch/cp02_landlock_policy_20260910/portable_suite_integration.receipt.json`.
These results do not certify later adapter or serializer edits.

The retained-data serializer field-order defect was corrected. An actual
full-grid body driver now passes export -> canonical OFF -> LAPSPREP consume
and equality, with 128 variable arrays and metadata equal before/after except
the declared global stage. Evidence:
`scratch/cp02_e02_adapters_y56nusbb/BODY_PASS_STATUS.json`.
This used the earlier partial-LGT inputs and a body driver; full maintained
caller/ELF cutover awaits final serializer metadata validation. The new original
LGT-inclusive inputs are available for that next test. The earlier failed
payload probes remain preserved. Graph refresh remains pending this active
source wave; CP02 stays IN_PROGRESS/NOT_RUN.

The new actual-LGT original four-case products also feed eight pure-original
cold LAPSPREP baseline runs (O0/O2 per case). All eight exit zero, preserve input
inventories and produce case-wise byte-identical O0/O2 WPS files. The parent
independently verifies all eight recorded hashes and four byte comparisons.
Receipt: `scratch/cp02_original_e01_builds_20260910/original_lgt_lapsprep_baselines/RECEIPT.json`.
These are baseline runs only; the new integrated OFF and native comparison is
still pending. Exact cold control changes are confined to private copies and
recorded in each invocation. This runtime uses the earlier bounded main-ELF /
write-confinement path, not the new complete detached-runtime claim.

## Uninterrupted original replay and LSX comment repeatability limit

After correcting the wrapper's generated-file allowlist, all four cases ran
again from fresh exact initial snapshots through all six original producers
without intervention: 24 producer exits and all four wrapper exits are zero.
The final evidence is
`scratch/cp02_original_e01_builds_20260910/ORIGINAL_LGT_FINAL_FOUR_CASE_RECEIPT.json`,
linking each `original_lgt_final_HH_content_receipt_v2.json`.
All 104 NetCDF product checks pass and every numeric variable equals the prior
diagnostic chain. Only 101/104 complete files are byte-identical: LSX in 12–14
UTC differs in four character positions of ten QC-count comments. Global
attributes and all numeric arrays match; exact before/after comment bytes and
positions are recorded, without normalization.
Original `src/sfc/lapsvanl.f:1082` writes `n_sao_g,n_sao_b` into these positions.
The supplied counts have no assignments in the inspected `qc_data.f` output
arguments and are unused except as passed arguments in `mdatlaps.f`; a count
initialization defect is under independent review. No complete byte-repeatability
claim is made. Earlier failed wrapper/audit probes remain retained.

The diagnostic actual-LGT input inventories remain the selected integration
inputs paired with the already executed eight original cold baselines. The
final replay's slightly different LSX bytes are not silently substituted into
that comparison. Independent GREEN and RED are reviewing the native-LGT source,
replay declarations and both diagnostic/final evidence boundaries.

The serializer owner has frozen its final API after focused tests at
`scratch/stage_payload_tests.s9eTcY/receipt.txt`. Maintained adapter, main-caller,
coordinator and builder integration has now been applied; fresh full builds are
in progress. The new coordinator test is registered in the Python suite; the
previous 37-file suite result does not certify this latest source cutover.
The structural graph remains stale until the active source wave is stable.

## First actual full-ELF canonical OFF chain

The fresh maintained O0 derived export, QBAL OFF and LAPSPREP OFF executables
now pass on the selected original actual-LGT 12 UTC input inventory. The
coordinator detaches input/runtime bytes, seals executable/loader FDs and grants
read-only access to inputs/context/runtime with writes only to stage output.
All three launches exit zero with exact post-input hashes. The parent rechecks
those receipts and confirms the 28,756,512-byte WPS output is exactly equal to
the selected original 108-record baseline (SHA256
`05e56d2ddf6df49d1a0faf5699c08a02fdfc0d8b7860fadca427db06f4e5d3a5`).
Evidence: `scratch/cp02_e02_adapters_y56nusbb/full_export_chain12.nt_3tksj/RECEIPT.json`
and `PARENT_RECHECK.json`.
This starts from already terminated original products: it does not yet execute
producer generation and export through the same newly built derived ELF.
The ncgen child closure, full regression dependencies, additional cases/O2,
independent integration audits and native requirements remain open.

The same explicit full-ELF chain also passes at O2 for 12 UTC with exact original
WPS bytes (`full_export_chain12_O2.0dq_w2s4/RECEIPT.json` under the adapter scratch).
The new diagnostic actual-LGT four-case ordinary/research-OFF matrix passes
32 positive runs and the 12 UTC ten-negative matrix with freshly linked full
O0/O2 executables. Parent rechecks all 32 output hashes, all input inventories,
and the exact original 108-record fields in both 108- and optional 129-record
outputs. Evidence: `scratch/cp02_e02_adapters_y56nusbb/new_lgt_off_matrix.kncm1m44/RECEIPT.json`
and `PARENT_RECHECK.json`. This matrix is separate from explicit inter-stage
payload mode; the latter's 13–15 UTC O0/O2 extension is running.

Original producer build postchecks pass all 18 live source/runtime/executable
manifests. The old original LAPSPREP source manifests now fail only at the
maintained builder pathname because the new adapter cutover changed that recipe
after the baseline runs. Its exact expected old bytes remain preserved as
`maintained_builder.before.sh` (SHA256 `7a39fffefe45a7fadf68aa659100ae898ab0838dd49ce99b9916b24e9d9d291a`).
All compiled sources, runtime files and executables remain exact. This is an
explicit historical-recipe mapping, not a rewritten/rebound launcher or a current
live-manifest PASS. See `scratch/cp02_original_e01_builds_20260910/final_build_postchecks_r2/RECEIPT.json`.
The latest 38-file Python suite passes with its exact source receipt in
`scratch/cp02_e02_adapters_y56nusbb/parent_python_cutover.receipt.json`; full Intel
unit verification is still running.

## Four-case full-ELF matrix and final unit result

`FULL_ELF_PAYLOAD_MATRIX.json` and `PARENT_FULL_ELF_MATRIX_RECHECK.json` in
`scratch/cp02_e02_adapters_y56nusbb/` now cover all four cases at O0/O2: eight
chains, 24 actual executable calls, 128 exact variable arrays and variable
metadata per pre/post pair, and eight WPS whole-file matches to original cold
baselines. Stage/source/binary/input-manifest identity changes are explicit.
No same-new-derived-ELF producer phase or descendant-termination closure is
claimed for these historical coordinator executions.

The complete pinned-Intel unit suite exits zero, including new payload and
adapter dependencies and both fixed-form QBAL compile gates. The 7,212-line log
SHA256 is `7b70ad3d77ff18f5549692be40b296c504a359ee155111b4952c2ef9985a46c5`.
`parent_full_unit_cutover.receipt.json` records the source observation boundary:
compiled Fortran and unit runners stayed unchanged, while four separate runtime/
ncgen helper/test files changed concurrently and require their own final tests.
The passing earlier Python38 receipt does not certify those later runtime edits.

Independent native-LGT RED is PASS_SCOPED with no material blocker:
`scratch/cp02_cloud_red_lgt_final_audit_20260910.4G8s4n/RED_REPORT.md`, SHA256
`591327373942de0d90f9a965d949dc2659fe604e1165ee0402dbd36f494dd5f9`.
It verifies six LGT and 36 replay tests, actual fixed-column content, alias and
malformation rejection, four final chains and the exact LSX comment limitation.
Independent GREEN and the new runtime/adapter RED audits remain in progress.
The ncgen bridge owner has completed strict direct-exec/real-byte-equivalence
and compile-only derived linkage; actual same-ELF production is still pending.
The tested dedicated-supervisor descendant closure is now being integrated.
CP02 remains IN_PROGRESS/NOT_RUN, with science/native/operational authority absent.

Independent native-LGT GREEN also finishes PASS_SCOPED:
`scratch/cp02_original_lgt_green.gJ2gJj/GREEN_LGT_AUDIT.md`, SHA256
`74b71546a3041cf0f1f32aa5066e5680c838a46bb9c9f105125ca0078bc75f9c`.
It independently rechecks all 104 products, exact numeric/schema/attribute
comparisons, four real LGT files, the 14-product original/corrected derived
comparison and preserved historical source mappings. The parent index is
`scratch/cp02_original_e01_builds_20260910/INDEPENDENT_LGT_AUDITS.json`.
Both independent teams retain the LSX comment and full-closure limitations.
Independent GREEN has moved to the actual full-ELF payload matrix; core and
runtime RED reviews are likewise ongoing. Before further builder changes,
361 compiled text-source identities from the full-ELF receipts were copied and
hash-verified into read-only content-addressed snapshots:
`scratch/cp02_e02_adapters_y56nusbb/full_elf_source_archive.ft9k7sqa/RECEIPT.json`.
No requested version was missing; compiler/runtime binaries remain separately
bound by existing receipts, so this is not a complete E06 archive.


## Same-ELF producer, supervised runtime and graph refresh

Evidence root: `scratch/cp02_e02_adapters_y56nusbb/`.
`SAME_ELF_TWO_PHASE_RECEIPT.json` records one actual derived O0 producer,
two EXPORT calls through that same ELF, and QBAL/LAPSPREP at O0 and O2.
All seven invocations terminate with leader success and supervisor ECHILD.
Each payload pair preserves 128 arrays and each WPS output equals the original
108-record cold baseline byte-for-byte. This extends only 12 UTC; the earlier
four-case matrix consumed previously generated products.

`SAME_ELF_PRODUCT_COMPARISON.json` is explicitly FAIL: 13/14 generated products
are byte-identical, but LCO COM differs at 128,639 cells, maximum valid absolute
difference 12.423933 Pa/s. The valid mask is unchanged. Transport/OFF success
does not waive this original-product discrepancy. An identical-input repeat and
source/build comparison are underway; no equation or numerical tuning has been
applied to make the comparison pass.

`RUNTIME_DELTA_FINAL_RECEIPT.json` binds the current seven maintained runtime,
coordinator and test files. Parent independently rehashed all seven successfully
in `PARENT_RUNTIME_FINAL_PIN_CHECK.json`; `git diff --check` passed. Coordinator
11 tests and an actual ifx smoke pass. The ncgen regression first reproduces
resolved-basename SONAME fallback, then verifies both detached runtime loader
lists and actual supervised Landlock execution with byte-identical generated
output. These focused results are separate from the earlier full Intel unit log.
The failed runtime attempts remain in `SAME_ELF_ATTEMPT_INDEX.json`: the loader
fallback was rejected; generated surface-count TMP was subsequently classified
as an auxiliary output while scientific inputs remained read-only.

Structural refresh evidence is in
`scratch/cp02_completion_ui53t82f/graph_latest_update/`.
Graphify updated 35 code files; unchanged configuration restored exactly
134 nodes and 133 incident edges before no-force clustering. Final graph:
4,612 nodes, 9,688 edges, 302 communities; SHA256
`8f4b754907be98b787bd246452bc976a18d55433a431733b5caa8416e9202fb7`.
Three source captures match across 307 files and integrity checks report no
malformed/dangling/duplicate edges. No semantic document re-extraction occurred.
CP02 remains IN_PROGRESS/NOT_RUN; full publication DAG, remaining actual cases,
observational authority and downstream native acceptance are not closed.

Parent final portable registry rerun after the runtime delta passed all 38 test files (exit 0): `scratch/cp02_e02_adapters_y56nusbb/parent_python_runtime_final.receipt.json`. ICX/ncgen remains in its separate pinned Intel gate.


### COM mismatch isolated to omitted runtime controls

An identical private ELF/input repeat reproduced all 14 output files exactly.
The original run used GA SC=0.10, ST=0.017, CT=1.3 and CU=0.5; the first private
producer omitted these variables and used the legacy defaults. No physics code
or numerical tolerance was changed. The corrected private caller pins the full
replay specification, supplies its eight accepted environment controls, and
checks the four printed VV controls against the original per-case log.
`DERIVED_CONTROL_AUDIT.json` inventories literal environment accesses in the
compiled source closure. The generic coordinator already records explicit
caller environment; this is a corrected caller contract, not inherited ambient
authority.

The controlled 12 UTC producer terminates with leader 0 and supervisor ECHILD.
Parent independently compares all 14 file SHA256 values, including LCO COM,
against selected original products: all exact. Evidence:
`scratch/cp02_e02_adapters_y56nusbb/PARENT_CONTROLLED_PRODUCTS12.json`.
The initial `SAME_ELF_PRODUCT_COMPARISON.json` remains a valid failed comparison
for its omitted-control inputs. Cases 13–15 and a controlled downstream rerun
are pending; no retrospective change to the initial receipt is made.

All four controlled derived O0 producers now pass: parent independently rehashes 56/56 current products against the selected original diagnostic products, all current scientific input inventories and all four supervised lifetimes. Evidence: `scratch/cp02_e02_adapters_y56nusbb/PARENT_CONTROLLED_FOUR_CASE_PRODUCTS.json`. Controlled same-ELF downstream matrix is in progress; initial omitted-control failures remain preserved.


### Final controlled four-case same-ELF matrix

`MATCHED_CONTROL_FINAL_INDEX.json` is the authoritative final index under
`scratch/cp02_e02_adapters_y56nusbb/`. It binds nine evidence/runner files.
`MATCHED_CONTROL_FOUR_CASE_RECEIPT.json` SHA256:
`b6bca7008ebe5d4b8e69c4a64529f55f507d37d0f792e18feda6c7eb8292137a`.
Four actual derived O0 producers, eight EXPORT calls through the same ELF,
eight QBAL OFF calls and eight LAPSPREP calls all terminate successfully with
supervised ECHILD (28 invocations). Downstream uses O0/O2; derived remains O0.
All eight canonical payload comparisons preserve 128 arrays and metadata, and
all eight WPS outputs equal their original 108-record cold/unbalanced baselines.
The configuration identity now includes the pinned original controls, LAPSPREP
configuration and producer receipt. This does not extend to populated radar LOS,
nonzero dynamic targets, balanced/hotstart, native output or publication authority.

Parent independently verifies nine final-index hashes, all 24 adapter receipt
digests and lifetimes, and eight WPS bytehash pairs in
`PARENT_MATCHED_CHAIN_IDENTITY_CHECK.json`. The separate parent four-case producer
check establishes 56/56 exact product bytes and unchanged inputs. Independent
GREEN/RED audits remain pending; historical omitted-control failures and earlier
unsupervised matrix receipts retain their original scope.

Independent E02 GREEN finishes PASS_SCOPED for the historical four-case full-ELF matrix, separate32-positive/10-negative OFF matrix, six build identities,361 archived source objects and scoped unit/runtime evidence. Report: `scratch/cp02_e02_green._r4y8_wm/REPORT.md`, SHA256 `636174fbe3745171e38da1896b9b47772b650505b56ab9027da854b21288b368`; parent binding is `scratch/cp02_e02_adapters_y56nusbb/PARENT_GREEN_E02_AUDIT_INDEX.json`. The newer matched-control28-invocation matrix is explicitly outside this first audit and receives a separate delta review.


## Actual O2 producer extension

Root built the unchanged private ncgen recipe at O2 in fresh pinned-Intel scratch
`upstream_derived_build.eamebg`. Evidence root:
`scratch/cp02_same_elf_O2_f21775dy/`; terminal build, producer, chain and independent
check logs are retained. `FINAL_RECEIPT.json` records four actual O2 derived
producers followed by four same-O2-ELF EXPORT, QBAL O2 and LAPSPREP O2 chains.
All 16 invocations terminate with success and supervised ECHILD. The four input
inventories remain unchanged; 56/56 products equal the selected original derived
O0 diagnostic products byte-for-byte (`PRODUCER_COMPARISON.json`). This explicitly
states the comparator optimization rather than inventing original-derived O2 runs.
All four pre/post payload pairs preserve 128 variables, arrays, dimensions and
attributes, with only the four declared identity changes; all four WPS files
are byte-identical to the selected original LAPSPREP O2 baselines.

This supplements the earlier controlled O0 producer matrix. It remains the
cold/unbalanced OFF path; no dynamic authority, native or publication claim.
`PRODUCER_EVIDENCE_ARCHIVE.json` verifies durable copies of 20 producer evidence
files; actual product bytes also remain in workspace chain snapshots. Independent
GREEN receives this as an optional separate delta after its historical audit.

The independent runtime-control RED report
`scratch/cp02_e02_adapters_y56nusbb/same_elf_control_red_review.md` finds no remaining
GA mismatch or byte-level blocker in the refreshed O0 matrix. Its follow-ups are
explicit absent controls, original KMP/MALLOC launcher provenance and stage
applicability, and direct ncgen identity binding. These are being incorporated
in the new E06 bundle; old omitted-control receipts are preserved and current
selection is via `MATCHED_CONTROL_FINAL_INDEX.json`.


Independent E02 RED confirms current payload wiring, direct ingress and
mask/support/geometry/unit/OFF gates, six pinned scratch ELFs and the controlled
28-call matrix. Report: `scratch/cp02_exit_red.e02_20260910_r1/CP02_E02_PAYLOAD_RED.md`,
SHA256 `a0263c7d2d6e6bd5102dac309781efe44721d2e0fe1d5700725d1daeaf53da30`.
Parent requested a separate scope addendum for its claim that CP02 E02 requires
production deployment: the CP02 isolated checklist and E02 row require actual
pinned full binaries and runtime evidence but do not name ANAL/EXET replacement
as an exit condition. The operational audit's38 blockers remain valid for those
deployed artifacts; they must not silently expand this authorized scratch work
into operational cutover. Whole CP02 remains incomplete for the separately
identified original exit requirements. The addendum also distinguishes actual
pure-original producer builds from maintained overlay comparisons.

Independent RED scope addendum resolves the deployment interpretation: `scratch/cp02_exit_red.e02_20260910_r1/CP02_E02_PAYLOAD_RED_SCOPE_ADDENDUM.md`, SHA256 `0df7b7e5941d21598ef42c9071a36a532a6d2b5f80c63409a3af9b47445f9a2a`. No E02/CP02 line requires installation under ANAL/NE57/EXET or operational Makefile changes. The deployed audit remains an operational/P6 limitation; actual pinned out-of-source binaries, canonical symbols and source/ABI/runtime receipts are the isolated E02 requirement. Pure-original six-stage builds/receipts are distinguished from maintained-overlay regeneration. Formal E02 and CP02 statuses are not promoted by this correction.


## Prospective implicit link-input pinning

Root found external LOAD entries in actual link maps that were absent from the
original source/runtime manifests, including Intel static runtimes, startup
objects and linker scripts. Existing ELF/runtime success does not retroactively
prove those build-time bytes. The maintained builder now performs a discovery
link, preserves `compile_inputs.sha256` and `discovery_link.map`, records all
selected LOAD bytes before the accepted final link, appends them to the normal
input manifest, relinks and rejects any final input-set or byte difference.
`tools/pin_link_inputs.py` implements the check; five focused tests cover exact
repeat, changed static bytes, added/removed inputs, symlink retarget and malformed
or FIFO inputs. The portable registry now includes this test (39 files).

A fresh pinned-ifx derived O2 build passes both links and postchecks:
`scratch/upstream_derived_build.ezqOQf`. Receipt:
`scratch/cp02_link_input_pins_qj349ens/FINAL_RECEIPT.json`.
It pins 38 distinct selected link paths; 20 external logical paths were absent
from the prior source/runtime manifest sets after resolving aliases. Some are
linker scripts or startup files, not additional runtime libraries. The old
builder bytes remain at `build_upstream_producer.before.sh` in that evidence
directory, SHA256 `7ce3d1ad7c2ac9b8ac08bc718aeba649dfc9fab94f29b3289fb8b65dd965080f`.
Historical source receipts must use their exact archived recipe, not the changed
live builder. This is prospective link-input evidence, not certification of all
compiler-process runtime/header/tool dependencies. Full E02 acceptance remains
unproven pending the complete dependency audit.

The E06 owner has a successful actual required-validation pilot in private tmpfs
`/tmp/off-publish12.7ndmd004/generations/case12-pilot`: selected self-contained
bundle2.1GB, snapshot validation, generation/current verification and semantic
recheck pass. Four-case failure/retry/collision/retained-FD tests and independent
RED remain underway. Historical implicit-link bytes in that bundle are explicitly
POST_BUILD_OBSERVED_NOT_PREBUILD_ATTESTED. Import-time validator runtime inventory
is separately retained at `scratch/cp02_validator_runtime_vu8t_zui/import_inventory.json`;
it is not an exercised full read-closure certificate.


The final portable39-file regression passes after registering the new link-input
check and adding the E06 validator allowlist entry. Exact log/source pins:
`scratch/cp02_link_input_pins_qj349ens/PYTHON39_RECEIPT.json`.

Four source-bound OFF profile figures and per-cycle input/output/source/runtime
receipts are retained under `scratch/cp02_off_profile_figures_1nk21rq1/INDEX.json`.
They show temperature and separate COM evidence means before/after OFF, with COM
valid-cell coverage; plotted arrays/masks and pressure/time agree exactly. The
12 UTC layout was visually inspected. Figures confer no dynamic target or native
authority; observed plotting runtime dependencies are not a full read-closure
certificate. The E06 owner will bind figures to the exact payload objects in new
final generations without modifying prior committed pilots.

The E06 owner's actual four-case tmpfs protocol v1 passes required trusted
validation, bad-control rejection with prior current preserved, retry, retained
staging-FD isolation, no-replace collision, interrupted rename/recovery and
reused-ID refusal. Final tests move to a new owned `/var/tmp/cloud_bal_e06_8udy3zls`
overlay root whose capability preflight passes. This is a selected scratch backend;
workspace Lustre remains correctly rejected. Neither result proves physical
power-loss durability or grants operational publication authority.


## Latest independent reviews and E01 remediation

Matched-control GREEN is PASS_SCOPED for28 supervised O0-producer calls,
56/56 products, eight128-variable payload and108-record WPS comparisons:
`scratch/cp02_e02_matched_green_20260910/MATCHED_CONTROL_GREEN_REPORT.md`.
The independent O2 extension review also passes4 producers/12 adapters,
56 exact products and four payload/WPS comparisons:
`scratch/cp02_e02_same_elf_o2_green_20260910/O2_GREEN_REPORT.md`.
Parent records report hashes in `scratch/cp02_e02_adapters_y56nusbb/PARENT_LATEST_AUDIT_INDEX.json`.
Historical build/transaction source drift is retained, not relabeled as current.

E01 completion RED found a concrete VRT hash-then-reopen race and missing
executable negative gates for missing producer, stale COM and same-valid-time
other cycle. Report: `scratch/cp02_cloud_red_e01_completion_audit_20260910.wFMnS8/REPORT.md`.
The VRT fix now reads hash-verified bytes and uses NetCDF memory I/O in the planner;
37 replay and6 LGT tests plus blocked dry-run assertions pass. Follow-up:
`scratch/cp02_cloud_red_e01_vrt_fix_20260910.piQEZE/FOLLOWUP_REPORT.md`.
The remaining executable cycle/freshness/coverage/exit gates are being implemented
without altering original NetCDF product bytes or converting engineering masks
into observational authority.


## Current maintained O0/O2 matrix and publisher RED fixes

The maintained builder now also carries the derived-only direct ncgen bridge.
An initial new build passed compile/link but its launcher rejected logical linker
aliases at canonical source preflight; no producer ran in that attempt. Its logs
remain `maintained_derived_O0.initial_alias.log`,
`maintained_derived_O2.initial_alias.log`, and `initial_alias_rejection.log` under
`scratch/cp02_link_input_pins_qj349ens/`. The helper now records canonical physical
LOAD paths. New builds were created; no old launcher or manifest was rebound.

All six fresh maintained derived/balance/LAPSPREP O0/O2 builds pass. Parent verified
every compile/link/runtime/executable manifest record against current bytes,
compile-plus-link manifest inclusion and discovery/final map equality. Eight
actual producers and24 downstream calls terminate0 with ECHILD;112/112 derived
products match selected original bytes. Independent parent checkers compare each
of eight128-variable payloads, including dimensions, types, attributes, all arrays
and the four intentional global identity changes. All eight WPS files match their
original baseline bytes. Combined evidence:
`scratch/cp02_link_input_pins_qj349ens/MAINTAINED_MATRIX_FINAL_RECEIPT.json`, SHA256
`3a04f94b5305b6ba057a10b36290d8e8ee4675d678cbeeaab3fa34472eb2fbea`.
Independent GREEN reports `scratch/cp02_link_input_pins_green_20260910/REPORT.md`
and `SMOKE_DELTA_REPORT.md` are PASS_SCOPED. Full compiler-process/header/tool
closure remains separately audited; these results do not close native/coupled
acceptance or whole CP02.

Eight newly rendered profile figures bind these exact current O0/O2 payload
hashes. Their indexes are `scratch/cp02_maintained_figures_O0_zdlgzq75/INDEX.json`
and `scratch/cp02_maintained_figures_O2_cazrsuuy/INDEX.json`. All figure receipt,
input and output hashes were rechecked; O2 12 UTC was visually inspected. Prior
figures retain their original older payload bindings and are not relabeled.

Independent E06 RED reproduced two blockers in the pre-fix publisher: a forged
PASS receipt with the actual trusted validator name/hash could commit a
semantically invalid snapshot, and a baseline receipt changed to FAIL passed
semantic verification after consistent content-address updates. Report:
`scratch/cp02_exit_red.e06_final_20260910/CP02_E06_OFF_RED_FINAL.md`.
Pre-RED generations and receipts remain historical evidence; their successful
normal runs do not prove a non-bypassable semantic gate.

The publisher now independently executes the allowlisted semantic validator,
and the OFF validator checks successful original baseline execution and exact
receipt types while preserving the legitimately false full-native/CP02-acceptance
flag. Source pins at this post-RED boundary: transaction
`8e11b376dec75a20de0aa791dd48f907ad10c7bce4941527a00d1a2a8b2a6659`, validator
`c5b6891516043e0a69cef5cd4f99a8f58059affa86d18680e8b432b43bb29552`.
Focused16 checks, output transaction tests and25 recovery tests pass. Four
transaction-mechanics fixtures explicitly inject and assert a test semantic
dispatcher; the real forged-PASS regression and actual publication protocol use
the real allowlisted dispatch.

A fresh four-case actual post-RED protocol passes at owned overlay root
`/var/tmp/cloud_bal_e06_redfix_zd9fskdi`; final current is
`generations/case15-redfix-recover`. Evidence:
`scratch/cp02_e06_stage_off_20260910/POST_RED_INDEX_PENDING.json` and
`POST_RED_PROTOCOL_RECEIPT.json`. It includes invalid-control rejection preserving
current, retry, retained staging-FD isolation, collision refusal, interrupted
rename/recovery and duplicate-ID refusal. Independent RED final review is pending.
These four bundles use the previous matched-control matrix, with historical
implicit links explicitly post-build-observed. A separate eight-bundle integration
of the newest maintained matrix, prospective link pins and fresh figures is in
progress. Neither generation set certifies power-loss durability, exhaustive
Python startup reads, dynamic observational authority or full CP02 acceptance.

The earlier39-file portable receipt precedes the canonical helper and latest
transaction/validator changes; it remains evidence for its exact recorded source
boundary. Structural graph refresh is pending these source changes and the E01
runner gates. CP02 remains IN_PROGRESS/NOT_RUN.


The post-RED portable regression now passes all39 registered files on an unchanged
tools/tests source boundary: `scratch/cp02_post_red_portable_evqb1041/RECEIPT.json`,
log SHA256 `966fb42f83df5564832a619945c673cdd996f3e5cf6a1051b59eb4d2c4891f5f`.
The current25 recovery tests exercise20 actual commit fsync boundaries; earlier
16-boundary receipts remain historical. Source capture and driver are retained.
The latest-matrix validator extension has not yet been substituted into this
receipt. A historical GREEN wording correction has an exact old-byte mapping in
`scratch/cp02_e02_adapters_y56nusbb/PARENT_GREEN_E02_HISTORICAL_MAPPING.json`;
the old parent index itself is preserved.


## Latest eight-chain publication: preserved orchestration/cache failures

The latest validator extension is SHA256
`21799dbd32500d4efb9514640f5d2926c6b50d8e0a02d93bb9a6f1b8094fba14`;
the focused gate is `be81ed27bd7ed1e1443a3d313a1568bc1075af34cd76894b9701cd091c4631c2`.
All eight new bundles were staged, and O0/O2 each pass20 real-bundle checks,
including four prospective-link negatives. The earlier39-file portable receipt
predates this validator extension.

The first eight-generation runner terminated1 after publishing four O0 cases at
`/var/tmp/cloud_bal_e06_latest_zgiwq_8y`. It then attempted12 UTC O2 after15 UTC O0;
the existing monotonic-current policy correctly refused the time regression.
Actual publisher dispatch also rejected a caller-crafted trusted PASS for invalid
controls. `scratch/cp02_e06_latest_matrix_20260910/PROGRESS.json`, `publication.log`
and the entire partial publication root remain preserved. The next fresh root
will interleave O0/O2 within ascending valid time; no time-policy bypass or
historical generation rewrite is authorized.

A separate post-validation runtime audit remains FAIL: two existing Python cache
files changed exact bytes while their source files remained unchanged. The
changed paths are transaction/validator cpython-312 caches; their code filename
representation differs. `VALIDATOR_RUNTIME_AUDIT.json` retains this failure.
This is not a demonstrated numerical difference, but the byte identity claim is
not accepted. A fresh private empty cache prefix and disabled bytecode writes
will establish an independent source-based validation context without deleting
shared caches or replacing byte hashes by normalized code hashes. No full Python
startup/filesystem-read closure claim is made.


## Ordered publication PASS and concrete compiler-process follow-up

The corrected ordered eight-generation protocol is terminal PASS:
`scratch/cp02_e06_latest_matrix_20260910/ORDERED_INDEX.json`, final current
`/var/tmp/cloud_bal_e06_ordered_k66lk931/generations/case15-O2`. It verifies actual
forged-control PASS rejection, retained FD isolation, collision/retry,
interruption/recovery and duplicate-ID refusal. The independent source-only
runtime audit records255 actual source/interpreter/extension/mapped-ELF leaves,
zero missing/changed, empty private cache and bytecode writes disabled. Its exact
context is `SOURCE_VALIDATOR_RUNTIME_AUDIT.json`; the old444-file cache FAIL is
preserved and is not overwritten by this result.

A separate explicit original-reference DAG delta is underway. Read-only audit
found5 unique missing original LOAD leaves per O0/O2 reference (about6.5MB each)
and its map; incidental content deduplication did not bind the relationship.
The new reference relation keeps POST_BUILD_OBSERVED_NOT_PREBUILD_ATTESTED scope.
It does not turn historical original linking into a prospectively pinned build.

Independent GREEN dependency audit:
`scratch/cp02_e02_dependency_gap_green_20260910/REPORT.md`, SHA256
`21fe9829b4a5126dabb5bc90087dcf5243a413b23f0213c48c5cf9b8d2c83394`.
It confirms current manifests but identifies concrete absent compiler child,
config, environment-script, system-header and binutils/runtime bytes. Root is
adding an audited build driver that discovers actual read/exec dependencies,
pins external regular bytes, then runs and verifies a second genuinely fresh
build. The existing builder gains supplemental precompile pins. The trace parser
and its focused tests are independently delegated; these new tools are NOT YET
validated by a full-build receipt. Historical builder/helper bytes are preserved
in `scratch/cp02_compiler_process_4t9f9hse/BEFORE_SOURCES.json`.

A fresh pinned Intel/C/ar/link probe passed with per-process file/exec tracing at
`scratch/cp02_compiler_process_4t9f9hse/probe/`. It directly observes the eight
sourced oneAPI environment scripts and compiler paths. This is parser/instrument
preparation, not a new full-build or producer acceptance.

The remaining actual experiment inputs and implementation boundary are recorded
in `docs/CP06_A_INPUT_READINESS_20260910.md`. The proposed13 UTC planning state
has0 valid target and0 valid sigma cells. Its exact hash is inspected; no target,
threshold, support or observational approval is invented. Existing experiment
specification location has been requested while engineering work continues.


The original-reference V2 eight-generation protocol is now terminal PASS, with
O0/O2 focused24 checks each and source-only255 runtime leaves exact. Final index:
`scratch/cp02_e06_original_reference_dag_20260910/FINAL_INDEX_PENDING_RED.json`,
SHA256 `fe8b774b071288ec38fd80460d0e1689513e595f89359e293d3132de57dbf8c0`;
final current is `/var/tmp/cloud_bal_e06_ordered_xk2btvik/generations/case15-O2`.
Parent rechecked the four maintained source pins; exact limited result:
`scratch/cp02_compiler_process_4t9f9hse/PARENT_E06_V2_INDEX_CHECK.json`.
Independent RED still reviews role-specific executed-artifact bindings; silence
is not PASS and the index explicitly remains pending independent review. The
new compiler-process tracing implementation is a separate, unvalidated delta.


## Independent E06 R2 disposition and targeted fixes

Independent report:
`scratch/cp02_exit_red.e06_latest_review_20260910/CP02_E06_PUBLISHER_RED_FOLLOWUP_R2.md`,
SHA256 `aff14440cfb0c8fceb572bc738d1bec1701346ffd533c7977b3675af258f3e12`.
The normal-commit forged PASS and invalid-baseline blockers are independently
CLOSED. Three current V2 attribution mutations still pass: swapping original
LOAD per-path digests, swapping maintained logical-to-resolved LOAD mappings,
and replacing a nonproducer helper artifact with coordinator bytes. Normal V2
data is consistent, but the validator's acceptance relation is insufficient.
The owner is adding exact path-preserving resolution/hash bindings and exact
helper/coordinator role equality to each actual launch receipt, with focused
pairwise/missing/extra-role regressions. All V2 sources and generations remain
preserved. Arbitrarily fabricated sealed generations are outside the declared
publisher-produced recovery boundary; no hostile same-account guarantee is made.

The compiler trace parser's first actual probe attempt failed on C-style mode
`0600`: `scratch/cp02_compiler_process_4t9f9hse/probe_parser.initial.log`.
This is a preserved draft-parser failure, not a full-build rejection or PASS.
The accepted two-build path must compare external bytes/path sets while emitting
separate trace receipts for distinct PIDs/generated roots; identical trace logs
are not expected. Source freeze and a real audited full-build pilot remain next.


## Compiler-audited matrix and V3 independent exit

All six accepted builds complete with external input counts derived727/729,
balance696/698 and LAPSPREP669/671 for O0/O2. Independent GREEN verifies
manifest bytes, trace hashes, source/tracer guards, separate fresh directories,
retained compiler pins and accepted input-manifest inclusion:
`scratch/cp02_compiler_inputs_green_audit_20260910/REPORT.md`.
Selected exec/open provenance is the bounded claim; arbitrary filesystem reads
or native science are not inferred. The actual eight producer/twenty-four
adapter matrix and eight newly bound figures now pass parent checks; exact
receipt is linked in the current evidence section. E06 V4 will integrate these
bytes in new generations; V3 generations and all failed experiments remain.

E01 actual absent required executable is now separately exercised:
`scratch/cp02_cloud_red_e01_missing_exec_20260910.zQ2x9K/MISSING_EXECUTABLE_PROBE_RECEIPT.json`,
SHA256 `9afc84af317058cc12ffaa4c67b40924882062a23167625dbff85e5747136c29`.
It rejects before any stage or output/seal; independent review of this addendum
is pending. The earlier wrong-hash test retains its own narrower interpretation.
The E01 gate is now registered in the41-file portable suite; final source-frozen
full execution remains pending V4. E03 read-only design identifies that exchange
v1 explicitly rejects dynamic authority and that coupled transport needs a
separate versioned contract and native readback, not a relaxed OFF flag.


## E01 final RED findings and portable41 backend correction

`scratch/cp02_exit_red.e01_final_review_20260910/CP02_E01_FINAL_RED_FOLLOWUP.md`
independently rehashes all four complete final runroots and recomputes source
cycles. This closes the former final-seal/runroot provenance finding at the
recorded scope. The remaining actual runner issue is broad named-fault
`GateError` classification as `PASS_NEGATIVE`; a caller-supplied expected
predecessor also permits skipping VRT at the helper boundary. Both are assigned
for targeted fixes and unrelated-error/skip-stage regressions. The absent
executable probe remains valid new evidence awaiting final index binding.

The first portable41 launch stopped in its first11-test metadata file with six
errors because the parent runner set TMPDIR to workspace Lustre. Atomic
no-replace directory publication correctly rejected that unsupported backend.
Exact failed log/receipt and disposition are preserved in
`scratch/cp02_portable41_p0960k9m/`; source hashes did not drift. This is not a
41-file PASS or a reason to weaken filesystem checks. A new independent run uses
owned `/var/tmp` backend scratch, still with pinned Intel profile, private empty
Python cache and before/after source capture:
`scratch/cp02_portable41_supported_84k79eag/`. Its live handle is52918; final
status is not inferred before terminal completion.


The supported portable41 run is terminal0/PASS, all41 registered files executed
and no source drift. Parent independently checked the complete execution list,
terminal marker and log digest (`PARENT_CHECK.json`). Log SHA256:
`6695b70d7b7624dd3b65cc8667aea0d80b118bb73bdadad9b1c20009b2938b4e`;
source-map SHA256:
`76e62e6d56aafef7d462f1b646959530eca6558cbacf32a145f6a51dd6ad5510`.
This does not certify subsequent E01 or V4 deltas; compare final frozen source
bytes against the retained map and rerun affected checks where needed.


## E03 readiness identity and E01 absent-file addendum

The coupled transport gap/implementation boundary is recorded in
`docs/CP02_E03_COUPLED_TRANSPORT_READINESS_20260910.md`. Parent verified15 exact
source/contract hashes. The initial check caught one pipeline hash transcription
error (`dd8af` versus actual `ddaf8`), corrected only in the document; no compiled
source changed. Both initial failure and final PASS source checks are preserved
in `scratch/cp02_compiler_process_4t9f9hse/E03_READINESS_SOURCE_CHECK*.json`.
Final document SHA256:
`821e8ee495c27920b03bc99a1a1ee7bbf0cc836cc242de448c81c64bde89a35b`.

The absent-executable probe is now connected to its historical final gate in
`scratch/cp02_cloud_red_e01_missing_exec_20260910.zQ2x9K/E01_FINAL_ADDENDUM_INDEX.json`
(SHA256 `9de5b0af00e68f8f109ee2f519ac3c46745f27869e7b9c701fc8e401d6c27c19`).
Parent checked five referenced artifact hashes and exact equality of its prior
negative case rows with the authoritative matrix (`PARENT_ADDENDUM_CHECK.json`).
This closes artifact-index absence, not the separate E01 fault-causality and
predecessor enforcement fixes still in progress.

V4 source freeze is now exact: verifier
`4862490ee7288eba80568d127d5170a4dc12e1cb0253a7fb09303ffb27e14ff3`,
gate `68fdb1bb83078ed5a70e352ca3b067e9fcb59025484b27ac9244078654d5ae03`.
`scratch/cp02_e06_compiler_binding_20260910/SOURCE_FREEZE.json` binds these plus
the private stager. The initial V4 bundle predating the final draft correctly
failed source binding and remains preserved (`focused_O0.first.log`). Fresh
12 O0/O2 bundles now exist; focused gates and13–15 staging are active. No PASS
is inferred from staging alone.


V4a focused O0/O2 gates both terminate1 on an actual positive-bundle verifier
ordering bug. The compiler manifest is sorted by Path components; the validator
incorrectly compares the precompile check log with lexically sorted full-path
strings. Parent independently reproduced727 exact manifest-ordered lines and
the first mismatch at index548 (`bits/types/FILE.h` vs `bits/types.h`):
`scratch/cp02_compiler_process_4t9f9hse/V4_PRECHECK_ORDER_PARENT_REPRO.json`.
The fix will preserve exact manifest order/content and duplicate rejection;
it is not a waiver or an actual build-input mismatch. Owner preserves V4a
sources/bundles/logs and will use fresh V4b roots after the staging wave ends.

The parent transaction/recovery regression passed with no source drift against
V4a verifier486249. Exact source-bound receipt:
`scratch/cp02_v4_transaction_regression_c2tftxj3/RECEIPT.json`, log SHA256
`b735250912ea7c0be5dd3728fd9fd9c32d1ff28afbdef0ccc71ce310746daee0`.
That PASS does not supersede the focused positive failure or certify V4b.


## V4b and E01 causal validation progress

V4b corrects precheck ordering and requires the exact successful exec pathname.
Verifier SHA256 is
`ac40cec79dac424b75b1a091cf4d124c91cbfe2a35ae1d592e0eacebd93b07df`;
gate remains68fdb1bb. The exact freeze is in
`scratch/cp02_e06_compiler_binding_v4b_20260910/SOURCE_FREEZE.json`.
All six actual compiler DAGs pass direct audit using this verifier; these are
immutable V4a input DAG bytes and do not replace fresh V4b bundle/publication
validation. Fresh eight-bundle staging is active (owner handle27431).

Parent E01 causal/predecessor and replay regression passed with no source drift:
`scratch/cp02_e01_causal_regression_qjye7vjp/RECEIPT.json`, log SHA256
`34c0534c66a2e4d6857f2ad55014379b9418aa362eca5f76d82c7086d60f9420`.
That run binds gate4471bc23 and test97252d10. During the following addendum
inspection, the owner was still supplementing tests: test identity changed to
c0dbed82 and the provisional addendum was updated. Parent stopped final-index
acceptance and requested an explicit final freeze. The observation is retained
in `scratch/cp02_compiler_process_4t9f9hse/E01_CAUSAL_INDEX_PARENT_CHECK.initial.json`.
No earlier positive run or parent regression is rebound to the new test bytes.


V4b review identified a contract-downgrade branch inherited from V3: a bundle
claiming legacy post-build scope could bypass new compiler obligations. Root
confirmed that the newest compiler-audited contract must require prospective
scope, with an explicit downgrade negative. Owner will finish live frozen runs,
preserve V4b evidence, then remove this branch before the next accepted source
freeze. Older V3 generations remain tied to their retained validator bytes;
this change does not rewrite or retroactively certify historical bundles.


## Final E01 freeze and final portable41

E01 owner final index is
`scratch/cp02_cloud_red_e01_causal_fix_20260910.mR7k4P/E01_CAUSAL_FIX_FINAL_INDEX.json`,
SHA256 `fb56858b5b5829c979953516101e02a6422ca080738e2e2d031af1e131877f3e`.
Gate4471bc23 and tests c0dbed82 are frozen;11 focused tests pass. Parent checked
18 exact referenced files and frozen/live source identities:
`scratch/cp02_compiler_process_4t9f9hse/E01_CAUSAL_FINAL_PARENT_CHECK.json`.
Historical four-case positive runs stay bound to f3c2; final RED delta review
is pending. Five preflight fault cases are present; the owner's message saying
“four” while listing five is a count typo, not a sixth execution.

The final E06 contract now requires prospective build scope and compiler audit
for every stage; three downgrade/omission negatives were added. Frozen verifier
`cc022dbaeaa28b22ad81f6f5d3627204fe040c615aac5719727d6e77fe34b78d`
and gate `7ee97b3da9b4fe663fcde422c51f9a60f9baa06811c84a3ce577b9da07622aec`
are bound by `scratch/cp02_e06_compiler_final_20260910/SOURCE_FREEZE.json`.
New57-check O0/O2 gates and fresh eight-bundle staging are active.

The complete final41-file portable suite passes against those frozen sources
and final E01, with no source drift. Exact receipt/source snapshots:
`scratch/cp02_final_portable41_ewyxj9vt/RECEIPT.json` and `source_snapshot/`.
Log SHA256 `b96ad3b7b3d0c125baaf520eff96e376b3f5b909e93a5b019e126075c902235b`;
source map SHA256 `3b0bccacafb8f89c9a2dc96b29fb589da208a542081cc7645a185c20467bcfb2`.
Parent verified all41 execution entries, log hash and every copied source hash
(`PARENT_CHECK.json`). A source-stable structural graph update is now active,
preserving the prior configuration graph; semantic document extraction is not
requested. Full CP02 remains incomplete pending E06 final protocol/audits and
actual CP06-A/native requirements.


## Frozen independent matrix and E01 causal audits

Matrix GREEN is final:1072/1072 checks,6 accepted builds,8 producers/112 products,
24 adapters,8×128-variable pairs,8 original WPS byte matches and8 figure bindings.
`scratch/cp02_compiler_matrix_green_audit_20260910/REPORT.json` SHA256
`70bcfc200796d84d85282cc0e3db35158041aea94a7d175d6fd51e6a48725f59`.
E03 planning/source audit also passes47/47 while explicitly leaving E03 OPEN:
`scratch/cp02_e03_green_audit_20260910/REPORT.json` SHA256
`76990ea780aacbecee6a578592c28631e025612a5c62ff1c5a5691bea9aa1131`.

E01 independent final RED confirms both causal-classification and predecessor
blockers closed, verifies final index/source snapshots, independently replays
five producer-free negative cases, and passes11 final gate tests:
`scratch/cp02_exit_red.e01_causal_final_20260910/CP02_E01_CAUSAL_FIX_RED_FINAL.md`.
The historical positive four-case source boundary and declared launcher/read
closure limits are retained; no scientific/native/operational authority follows.
Parent hashes all final audit artifacts in
`scratch/cp02_compiler_process_4t9f9hse/FINAL_INDEPENDENT_AUDITS.json`.

E06 final O0/O2 gates both terminate0 with57 checks and all8 bundles are staged.
The owner starts an actual fresh overlay publication/recovery protocol; staging
and focused checks alone are not publication evidence. Independent E06 RED is
reviewing the final frozen contract. GREEN is also auditing the original
requirement-level CP02 exit matrix to distinguish achieved evidence from
remaining native/authority/implementation work without requiring deployment.


## Final structural graph source check

Graph refresh outputs exist in
`scratch/cp02_completion_ui53t82f/graph_final_update/`. Parent verified316 source
hashes equal before/update/final/freeze captures and current bytes; live graph
bytes equal the retained final JSON. Counts are4782 nodes,10046 edges,307
communities. All134 prior configuration IDs and133 incident edges remain;
exact content-preservation is the graph owner's separate check. Integrity
diagnostics show no dangling/missing/duplicate/collapsed edges. Parent receipt:
`scratch/cp02_compiler_process_4t9f9hse/GRAPH_FINAL_PARENT_CHECK.json`, graph
SHA256 `d4fe16172aac6e1e0892e65ef4ee8a23d672ca3cb93742681999c02c9e964b30`.
The refresh is structural only; new document semantics remain unextracted.

The final E06 actual protocol is now confirmed live as owner handle88684 at
`/var/tmp/cloud_bal_e06_ordered_3kl5k1ez`; supported filesystem preflight passes.
Both original-reference link inventories have zero missing leaves. No final
publication status is inferred until the process and indexed protocol finish.


The initial final-protocol harness exposes quadratic preparation cost: for
each of13,336 fixed-hash objects it re-runs full product-context validation.
Owner measured only4452 copied objects after several CPU-active minutes, with
no generation/current yet. Root authorized a deliberate performance stop and
a new private harness/root, preserving the original as
`PERFORMANCE_ABORT_NOT_VALIDATION_FAILURE`. This is based on diagnosed repeated
parsing, not observation timeout. The replacement may use an already validated
owned directory and strict64-hex exclusive/no-follow object creation; transaction
prepare, snapshot, trusted validation, seal/current and lifecycle tests stay
unchanged. Maintained/frozen validator and transaction sources are untouched.
Final status and termination receipt remain required from the owner.


## Optimized actual publication protocol

Old process833071 is confirmed terminated, launcher88684 exit143, no children,
current or generations; partial root is preserved. Exact owner disposition:
`scratch/cp02_e06_compiler_final_20260910/PERFORMANCE_STOP_TERMINAL.json`.
Parent independently checks PID absence and zero publication state in
`scratch/cp02_compiler_process_4t9f9hse/PERFORMANCE_STOP_PARENT_CHECK.json`.

The new protocol is live42445 (actual Python PID847570 observed CPU-active) at
`/var/tmp/cloud_bal_e06_ordered_h4qzglvj`, evidence owner
`scratch/cp02_e06_compiler_publish_20260910/`. Its private copy helper uses a
fresh validated directory FD, strict64-hex names and exclusive/no-follow writes.
Parent proves the entire publication loop and failure scenarios are byte-exact
unchanged from the original harness; only imports/private copy setup differ.
New harness SHA256 `2f1d5aedc552e86de362a983300d6e21116e4f5dfe660449e6e275cb342d697d`;
review/diff: `scratch/cp02_compiler_process_4t9f9hse/PUBLISH_COPY_DELTA_PARENT_REVIEW.json`
and `publish_copy_delta.diff`. Maintained sources/gates are unchanged.

The first actual generation12-O0 has published; progress binds MANIFEST SHA256
`a82945a2ce24bba4d962fce3fc1ce5bb9b31b0801f994624578ae44e3edede7f`.
The other seven and full retained-FD/forged-PASS/collision/recovery events remain
pending. This first publication is not final eight-generation acceptance.


Final actual protocol has completed3/8 publications (12 O0/O2 and13 O0).
A caller-crafted PASS for invalid controls is rejected by the actual trusted
semantic dispatcher; prior current is preserved. The13-O0 retained writable
staging descriptor is mutated after validation and again after publication,
while the accepted generation remains byte-exact. Remaining five publications,
collision/retry and final recovery are still running under live PID847570.

The accepted12-O0 source-only validator runtime audit passes255 observed files,
zero missing/changed and an empty private cache. Parent independently rehashes
all255 recorded files; receipt SHA256
`dce79cae92944c46023b511d5fcc40373db14b2ec55dbe87c2336755eea4e9d2`.
Owner artifact: `scratch/cp02_e06_compiler_publish_20260910/SOURCE_VALIDATOR_RUNTIME_INVENTORY.json`;
parent: `scratch/cp02_compiler_process_4t9f9hse/FINAL_RUNTIME_PARENT_CHECK.json`.
This remains the declared observed runtime subset, not additional filesystem
read/startup coverage.
